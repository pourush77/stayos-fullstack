# StayOS — PRD & Progress Ledger

## Original Problem Statement
User owns two GitHub repos:
- Frontend: `pourush77/stayos-web` — Next.js 15 monorepo (npm workspaces: `apps/web`, `packages/ui|theme|types|utils|config`)
- Backend: `pourush77/stayos-api` — NestJS + TypeORM + PostgreSQL

User asked us to complete pending pages & functionality, module by module, across both repos, WITHOUT pushing to their GitHub accounts (they use "Save to GitHub" themselves when ready).

## Environment (this container)
- **Postgres 15** — supervisor `postgres`, DB `stayos_dev`, user `stayos`
- **Backend NestJS** — supervisor `stayos_api`, port `8001`, mapped to `/api/*` on preview URL via k8s ingress
- **Frontend Next.js dev** — supervisor `frontend`, port `3000`, `/app/frontend` → symlink → `/app/apps/web`
- All 3 services now auto-restart via supervisor (`/etc/supervisor/conf.d/stayos.conf`).
- Backend `.env`: `PORT=8001`, `SESSION_IDLE_LOCK_MINUTES=1440`, `CORS_ORIGINS` includes preview URL.
- Frontend `.env.local`: `NEXT_PUBLIC_API_BASE_URL=<preview-url>/api/v1`.
- `next.config.ts` `allowedDevOrigins` extended for `*.preview.emergentagent.com` / `*.preview.emergentcf.cloud`.

## User Personas
1. **Owner** — Full authority incl. assigning OWNER role.
2. **Admin** — Full ops incl. user management (cannot assign OWNER).
3. **Manager** — Ops oversight.
4. **Front Desk** — Bookings, check-in, rooms, guests, housekeeping visibility, **billing (view + manage)** — collects payments at reception.
5. **Housekeeping** — Task-only.
6. **Maintenance** — Ticket-based.
7. **Accounts** — **Full billing + reports**.
8. **Read-only** — Dashboard-only.

## Sessions Ledger

### 2026-08-03 — Walk-in Group + Phase 5B room-card group context
- **Container restore**: reinstalled Postgres 15, dependencies, ran migrations + demo bootstrap, wired supervisor `backend` → NestJS, `frontend` → Next.js, `postgres` → pg_ctlcluster. Both services healthy.
- **Walk-in Group one-click flow (NEW)** — chat #7 spec, single-transaction endpoint:
  - `POST /properties/:propertyId/operations/group-holds/walk-in` (controller + service `createWalkInGroup`).
  - In one DB transaction: creates `group_bookings` (status `CHECKED_IN`, source `WALK_IN`), room-type inventory blocks, physical room assignments, `group_stays`, single `group_master_folios`, marks all selected rooms `OCCUPIED`.
  - Validates: property, date range, ready-status of every room, no reservation conflicts, no active-group assignment conflicts.
  - Groups per-room adults/children into per-type blocks automatically.
  - Frontend: `WalkInGroupModal.tsx` (Bookings page → "Walk-in Group" button). Live room availability by type, per-room occupant name/adults/children, deposit, notes. Success state shows GRP code + folio number + occupied rooms + link to master folio. On success refreshes in-house groups list on Bookings page.
- **Phase 5B — Room card group context**:
  - `RoomCard` on `/rooms` now shows green `GROUP · GRP-XXXXX` badge when the room's `groupContext` is populated.
  - `getRoomSubtitle` renders `${groupCode} · ${groupName}` for group-occupied rooms (previously just guest name).
  - Backend already provided `groupContext` on both room-board list & room-drawer detail.
- **Verified end-to-end**: created walk-in group `GRP-00002` via UI (rooms 301, 302), master folio `GFO-00002` opened, room cards show `GROUP · GRP-XXXXX` badges. Existing group `GRP-00001` (created via API) also visible on both Bookings in-house list and Rooms board.
- **Tests**: `npm test` in stayos-api → 38 suites / 252 tests all passing. `npm run build` passes. `tsc --noEmit` passes. ESLint clean on both modified files.


### Session 1 — 2026-07-30 — Bootstrap + Auth + Billing
**Environment**
- Cloned `stayos-api`; installed Node 22, Postgres 15; ran TypeORM migrations; seeded demo data.
- Registered Postgres + Nest under supervisor so they auto-restart if the container recycles them.
- Fixed enum-typing bug in `scripts/bootstrap/03-employees.js`.
- Added dynamic CORS to Nest via `CORS_ORIGINS` env var.
- Fixed Next.js hydration blockage by extending `allowedDevOrigins`.

**Auth section — DONE**
- User Management UI (`/settings/users`): full CRUD + admin password reset + activate/deactivate.
- Settings landing page (`/settings`) with 6 tiles (Users + Employees active; 4 placeholders).
- Forgot Password → "Contact your administrator" hint (no self-serve; passwords set hierarchically).
- Session idle lock bumped to 24 h backend side; no client-side lock modal per user request.

**Billing section — DONE**
- **Backend** (NestJS):
  - New module `src/core/billing/` with 3 entities: `FolioEntity`, `FolioChargeEntity`, `FolioPaymentEntity`.
  - Migration `1783924200000-CreateBillingTables.ts` creates 3 enums + 3 tables + FKs + indexes.
  - `BillingService` handles: list folios, get folio, get/create folio for reservation (auto-computes ROOM charges & tax on first open), add charge, add payment (auto-updates reservation `paymentStatus`), settle folio (only when balance ≤ 0), overview aggregation.
  - `BillingController` endpoints (permission-gated by `billing.view` / `billing.manage`):
    - `GET /properties/:pid/folios[?status=OPEN|SETTLED|VOID]`
    - `GET /properties/:pid/folios/:folioId`
    - `GET /properties/:pid/reservations/:reservationId/folio` (get-or-create)
    - `GET /properties/:pid/billing/overview` (KPIs)
    - `POST /properties/:pid/folios/:folioId/charges`
    - `POST /properties/:pid/folios/:folioId/payments`
    - `POST /properties/:pid/folios/:folioId/settle`
  - Registered in `AppModule`. `FRONT_DESK` role granted `BillingManage` so reception can collect payment.
  - Idempotent seed script: `scripts/bootstrap-billing.js`.
- **Frontend** (Next.js):
  - `/billing` — full folios list, 5 KPI stat cards (open, settled, outstanding, today revenue, this month), search + status filter, per-row "Open" link.
  - `/billing/[folioId]` — folio detail with charges & payments tables, action buttons (Add Charge, Collect Payment, Settle Folio), color-coded charge type badges, VIP badge on guest, Back to Billing breadcrumb.
  - Reusable `FolioPanel` component (`features/billing/components/FolioPanel.tsx`) drives both the folio detail page and the Stay Workspace billing accordion.
  - **Stay Workspace billing panel** replaced the "coming soon" stub with a real embedded folio panel and enabled the Collect Payment button (was hardcoded `disabled`).
  - Two modals: **Add Charge** (type/description/qty/unit/tax) and **Collect Payment** (method/amount/reference/notes) with live balance-due display + validation.
  - Currency formatted as `en-IN` INR everywhere.

**Verified end-to-end via curl + browser:**
Create folio → add F&B & minibar charges → record card payment → reservation `paymentStatus` auto-updates → list folios shows correct paid/balance → overview aggregates outstanding & revenue → `/billing` UI shows 4 folios with the seeded data & correct badge colours.

**Files added / changed in Session 1**

Backend (`/app/stayos-api`):
- `src/main.ts` — dynamic CORS.
- `src/app.module.ts` — registered `BillingModule`.
- `src/core/auth/role-permissions.ts` — FRONT_DESK gets `BillingManage`.
- `src/core/billing/**` — full module (entities, DTOs, service, mapper, controller, module).
- `src/database/migrations/1783924200000-CreateBillingTables.ts`.
- `scripts/bootstrap/03-employees.js` — enum cast fix.
- `scripts/bootstrap-billing.js` — new idempotent billing seed.
- `.env` — added `PORT=8001`, `CORS_ORIGINS`, `SESSION_IDLE_LOCK_MINUTES=1440`.

Frontend (`/app`):
- `apps/web/next.config.ts` — `allowedDevOrigins` for preview domains.
- `apps/web/package.json` — start script bound to port 3000.
- `apps/web/.env.local` — API base URL.
- `apps/web/src/features/users/**` — new (types, api, `UsersPage.tsx`).
- `apps/web/src/features/billing/**` — new (types, api, `FolioPanel.tsx`, `BillingPage.tsx`).
- `apps/web/src/features/stays/components/StayBillingPanel.tsx` — new.
- `apps/web/src/features/stays/components/StayWorkspace.tsx` — wired billing panel + auth-context.
- `apps/web/src/app/settings/page.tsx` — settings landing.
- `apps/web/src/app/settings/users/page.tsx` — Users route.
- `apps/web/src/app/billing/page.tsx` — Billing list route.
- `apps/web/src/app/billing/[folioId]/page.tsx` — Folio detail route.
- `apps/web/src/app/login/page.tsx` — "Forgot Password?" hint text.

Container:
- `/etc/supervisor/conf.d/stayos.conf` — auto-managed `postgres` + `stayos_api` services.

## Prioritized Backlog

### V1 — LAUNCH READY (2026-08-01) ✅
Full front-desk lifecycle certified for real staff use. See CHANGELOG.md 2026-08-01 entry.

### P1 — post-launch enhancements
- Guest Signature Capture (signature pad → save as GUEST_SIGNATURE doc).
- Persist Face Snap (webcam capture from Check-In → save as GUEST_FACE doc for audit).
- OCR Confidence indicators (🟢🟡🔴 next to auto-filled ID/Name/DOB fields).
- Housekeeping Inspect modal: pre-load the housekeeper's submitted checklist (currently starts empty).
- Housekeeping: surface API error toasts on failed mutations.
- Availability Calendar: empty-cell click opens New Booking prefilled.
- Availability Calendar: populate `roomTypeName` in payload (currently '—').
- Housekeeping board: refactor to smaller components + add data-testid coverage.
- Housekeeping staff access token endpoint should return 401/404 instead of 400.

### P2
- Maintenance module UI + tickets (backend module scaffolded, no UI).
- Amenities module (schema + APIs + denormalize into room types).
- Additional Guests table & unhidden UI.
- Audit trail viewer UI.
- Property / Preferences / Security / API Keys settings tiles.
- Rate/pricing engine (replace hardcoded default rates for accurate revenue reporting).
- Frontend tests setup (Vitest + MSW).
- Repo-wide lint cleanup (500+ errors in scripts/ + type imports).
- CheckIn workspace: native DoB input → Mantine DateInput.
- `/reports` duplicate React key warning.
- `/reports` "Expected today" copy for future arrivals.
- List envelope inconsistency (bare array vs `{items,...}`).
- Add proper data-testid attributes to /housekeeping board + Availability Calendar cells.

## Notes for future contributors
- For the user's local machine, `NEXT_PUBLIC_API_BASE_URL` should be their LAN IP (`http://192.168.1.31:3002/api/v1`) and backend's `PORT=3002` — different from this container's setup which uses `8001` to piggyback on the preview ingress `/api/*` route.
- `AUTH_ENABLED=true` — permission guard active.
- Folio numbers follow the pattern `FOYYMMDD-00001` scoped per property.
- Balance-based reservation `paymentStatus` sync happens inside `BillingService.addPayment` — no separate call needed from the frontend.
- The backend is compiled (dist/) and runs via `node dist/src/main.js`. Any TS change to `stayos-api/` requires `npm run build` + `sudo supervisorctl restart stayos_api`.
- `packages/ui/src/layout/stayos-app-shell.tsx` — must reference `process.env.NEXT_PUBLIC_API_BASE_URL` directly (Next.js only inlines literal references). Do NOT indirect via `globalThis`.

## 2026-08-16 — Property Settings: Group Booking Deposit
- Root cause: reported 400 "should not exist" came from a STALE API build (old UpdatePropertyDto without deposit fields); current source validates correctly. Real source defect: deposit fields missing @ApiPropertyOptional so absent from Swagger PATCH schema.
- Fix: added @ApiPropertyOptional to groupBookingDepositPolicyType/Value in update-property.dto.ts; requires nest build + API restart to clear stale build.
- Tests: stayos-api properties (22) + group-booking-deposit-policy (9) green; nest build passes.

## 2026-08-16 — group-room-mix-suggestions NONE+0 fix
- Root cause: normalizeGroupBookingDepositPolicy() rejected any non-null value for NONE, so persisted DB default (NONE, value 0) passed by group-room-mix.service caused 400.
- Fix (normalization only): NONE now accepts undefined/null/0 (normalize to 0), rejects >0; PERCENTAGE >0..100; FIXED_AMOUNT >0.
- Verified via testing_agent (iteration_4): real endpoint returns 200; 100% backend pass. No schema change/migration.

## 2026-08-16 — Phase-1 Property/Policy Foundation
- Added property_policies (deposit/cancellation/no-show/early-checkin/late-checkout; separable structured columns; rate_plan_id nullable + partial-unique for future hierarchy), property_billing_configs (invoice prefix/numbering + HSN/SAC), properties.business_day_cut_off_time + BusinessDateService, reservations.policy_snapshot/tax_snapshot (nullable foundation).
- Endpoints: GET/PUT /properties/:id/policies(/:policyType), GET/PUT /properties/:id/billing-config. Deposit reuses normalizeGroupBookingDepositPolicy.
- 4 migrations (20260817090000-093000) applied. Verified via testing_agent iteration_5 (36/36 after fixing foreign-ratePlanId 500->404). Jest: 18 policies + related green.
- Next-phase blockers: consolidate legacy properties.group_booking_deposit_* into property_policies (currently dual source); populate reservation snapshots at booking; rate-plan override read path + seeding.

## 2026-08-16 — Phase-1A: property_policies as authoritative deposit source
- Deposit calc (group-room-mix, group-booking) + property GET/PATCH facade now resolve from property_policies via PolicyResolverService (RatePlan override -> Property default). Generic normalizeDepositPolicy replaces group-specific one.
- Migration 20260818090000 backfilled GROUP_DEPOSIT rows and DROPPED legacy properties.group_booking_deposit_policy_* columns (single source of truth).
- Verified: testing_agent iteration_6 100% (18 new + 41 regression), + resolver/negative-NONE/rate-plan-scoped-GET fixes (59 pytest + 48 jest green).
- Phase-1B blockers: booking-time reservation policy/tax snapshot population; RatePlan CRUD to exercise overrides; wrap property base-save + deposit upsert in one transaction; consolidate DTO deposit rules into shared normalizer; batch-load deposits in properties list (N+1) when multi-property lands.

## 2026-08-16 — Phase-1A consistency: atomic Property PATCH + DTO shape-only
- Property PATCH now wraps GROUP_DEPOSIT policy upsert + base save in ONE transaction (deposit-first); invalid deposit or either save failing rolls back everything.
- Removed duplicated deposit business rules from UpdatePropertyDto (shape/type only); shared normalizer via PoliciesService.upsert is the single rule source.
- Verified: testing_agent iteration_7 100% (atomic rollback via API confirmed) + 139 jest green.
- Next: Phase 1B Reservation & Stay Lifecycle (Rate Plan CRUD deferred to 1C).

## 2026-08-16 — Phase-1B Reservation & Stay Lifecycle
- Added canonical transition map (reservation-transitions.ts) as single source of truth + confirm/cancel/markNoShow in reservation-workflow.service (assign/move/check-in/out/extend pre-existing).
- Confirm freezes immutable policy_snapshot+tax_snapshot (PolicyResolverService + TaxService). Cancel/no-show release the room-night hold (roomId cleared).
- Closed HIGH bypass: status removed from UpdateReservationDto (OmitType) -> transitions only via lifecycle endpoints. Fixed MEDIUM: generic PATCH FK/relation updates now reliable + response re-fetched.
- Verified testing_agent iteration_9 100% (42/42); jest reservations 74 green.
- Phase-1C blockers: source enum expansion (PHONE/CHANNEL/OTHER) + external reservation/confirmation IDs foundation NOT yet added; expose snapshots via API read model; Inventory + Rate Plans (availability/overbooking/ARI) own the override read path.

## 2026-08-16 — Reservation->Inventory boundary hardening
- Decision: PENDING = canonical HOLD (no separate HOLD). Inventory entitlement = property+roomType+dates+status (INVENTORY_CONSUMING_STATUSES=PENDING/CONFIRMED/CHECKED_IN); roomId is assignment-only.
- Removed the wrong "clear roomId releases inventory" assumption; cancel/no-show now release via releaseInventoryEntitlement() hook (RESERVATION_INVENTORY_RELEASED audit event, terminal-status driven) and KEEP roomId. Assign/unassign never triggers release.
- Verified testing_agent iteration_10 100% (53/53); jest reservations 78 green.
- Phase-1C blockers: implement the actual availability ledger inside the releaseInventoryEntitlement hook + reserve-on-confirm; room-type/date availability + overbooking + daily rates + rate plans + restrictions as single source; source/external-ID + snapshot read-model can fold into 1C.

## 2026-08-16 — Phase 1C-a1: Room-Type Inventory Foundation (standalone, NOT wired)
- New `room_type_inventory` entity/table: unique (property_id, room_type_id, date); DB invariants capacity>=0, sold>=0, sold<=capacity; FKs to properties/room_types (RESTRICT). Files: `core/inventory/infrastructure/room-type-inventory.entity.ts`.
- Semantics locked: capacity = STRUCTURAL room-type inventory (count of `rooms.status='ACTIVE'` for property+roomType; NOT reduced by DIRTY/MAINTENANCE/occupied/assigned — those are future OOO layers). sold = inventory CONSUMED (entitlement), not confirmed sales. available = capacity - sold. Physical roomId assignment never affects sold.
- Migration `20260819090000-CreateRoomTypeInventory.ts`: creates table + indexes + FKs, then deterministic backfill = one row per stay-night of every currently consuming reservation (PENDING/CONFIRMED/CHECKED_IN), capacity from active room count. Backfill NEVER clamps/inflates/silently-corrects: nights where sold>capacity are RAISE WARNING-reported and intentionally left un-backfilled for reconciliation to surface.
- `AvailabilityService` (`core/inventory/availability.service.ts`): read([start,end)), reserve(), restore(). Deterministic ascending-by-date `SELECT ... FOR UPDATE` locking. Concurrency-safe lazy row creation via `INSERT ... ON CONFLICT DO NOTHING` + re-SELECT FOR UPDATE (unique index is final safeguard; no duplicate-key surfaced). reserve throws ConflictException(INVENTORY_UNAVAILABLE) when available<units (no oversell); restore clamps at 0.
- `InventoryReconciliationService` (`core/inventory/inventory-reconciliation.service.ts`): read-only; recomputes expected sold/capacity from reservations+active rooms, FULL OUTER JOIN vs stored; classifies MISSING_ROW / ORPHAN_SOLD / SOLD_MISMATCH / CAPACITY_MISMATCH / OVERSELL. Never mutates.
- `InventoryModule` registered in AppModule. Added ApiErrorCode.INVENTORY_UNAVAILABLE. NOT wired into reservation lifecycle (no controller, no endpoint, reservation flows unchanged).
- Verified: nest build clean; `npm test` 501/501 (55 suites) green (incl. 20 new inventory tests: nights domain, availability service in-memory concurrency/last-room/no-partial-oversell/restore, reconciliation classifications). Fixed a pre-existing stale smoke spec (`hotel-inventory-api-smoke.spec.ts` was missing a PolicyResolverService provider added back in Phase 1A). Live DB check: migration backfilled 90 rows / 0 invariant violations; reconcile() surfaced the 4 demo oversold nights as OVERSELL+MISSING_ROW; AvailabilityService.read reports full structural capacity for future no-row dates. App boots cleanly (InventoryModule dependencies initialized).
- Phase-1C-a2 (next): transactional inventory consumption wiring (reserve-on-confirm) — still to be done in a later slice.

## 2026-08-16 — Phase 1C-a2: Entitlement-driven inventory consumption (creation + consuming-entry)
- Design correction adopted: inventory changes ONLY when the reservation entitlement crosses the consuming/non-consuming boundary — NOT "reserve on confirm". Shared pure helper `core/reservations/domain/reservation-inventory-transition.ts`: `inventoryDeltaForTransition(from|null, to)` -> RESERVE (enter consuming) / RELEASE (leave consuming, 1C-a3) / NONE (consuming->consuming or non->non).
- Integration point: `ReservationsService.create()` is now transactional (`dataSource.transaction`). Reservation row is saved and, iff `inventoryDeltaForTransition(null, status) === RESERVE`, `AvailabilityService.reserve({propertyId, roomTypeId, nights=expandStayNights(arrival,departure), units:1}, manager)` runs in the SAME transaction. Any night out of stock throws ConflictException(INVENTORY_UNAVAILABLE) -> whole tx rolls back (no reservation, no partial inventory). `ReservationsModule` now imports `InventoryModule`.
- confirm()/checkIn() DELIBERATELY untouched: PENDING->CONFIRMED and CONFIRMED->CHECKED_IN are consuming->consuming (delta NONE) so they naturally reserve nothing. roomId assign/unassign/move paths contain zero inventory calls by construction — roomId has no inventory effect. cancel/no-show/check-out RELEASE deferred to 1C-a3.
- No double-count: consumption is driven off the transition DELTA (from=null on create), never by replaying existing backfilled entitlements.
- Verified: build clean; full `npm test` 508/508 (56 suites) incl. new transition matrix spec + 3 create-inventory specs (reserve 1/night on consuming create, no reserve into non-consuming status, rollback on insufficient). Live DB e2e (throwaway script, cleaned up): (1) create PENDING -> +1/night; (2) confirm -> unchanged; (3) check-in gate hit but inventory unchanged; (4) insufficient -> INVENTORY_UNAVAILABLE; (5) 2-night booking with one night full -> full rollback, other night unchanged (no partial); (6) assign path has no inventory code; (7) no double count. 0 residue, 0 invariant violations after run.
- Transactional integration points (exact): `ReservationsService.create` (only consuming-entry site in the current state machine). Future non-consuming->consuming transitions must call the same delta helper inside their mutation transaction.
- Phase-1C-a3 (NOT started): RELEASE wiring for cancel/no-show/check-out + date-change/roomType-change entitlement adjustment via the same delta abstraction.

## 2026-08-16 — Phase 1C-a3: Entitlement release + date/roomType diff mutations
- Canonical release path: `ReservationWorkflowService.releaseInventoryEntitlement()` now performs the real `AvailabilityService.restore(...)` (in addition to the RESERVATION_INVENTORY_RELEASED audit event). Wired for cancel + no-show (via `terminate()`) AND check-out (added `getReservationInventoryEntitlement` capture before status flip + `releaseInventoryEntitlement` call). One canonical release path — no second restore mechanism. Double-release impossible: `assertReservationTransition`/`ensureReservationStatus` guards make terminal transitions run once.
- Shared diff abstraction `diffEntitlements(before, after)` in `reservation-inventory-transition.ts`: returns `{toRelease, toReserve}` InventoryKey[] (roomTypeId+date). Only disappearing keys release, only new keys reserve, overlap untouched; a roomType change transfers the whole entitlement (all old release, all new reserve) even on overlapping dates.
- New engine primitive `AvailabilityService.applyDelta({propertyId, toRelease, toReserve, units}, manager)`: locks EVERY affected (roomType,date) key — release + reserve — in ONE deterministic global order (roomType asc, then date asc) BEFORE mutating; validates all reserve keys have capacity; on shortfall throws INVENTORY_UNAVAILABLE so the caller tx rolls back reservation + all inventory (original entitlement intact); only then decrements releases / increments reserves. This global ordering (consistent with reserve()/restore() single-roomType date-asc) prevents cross-roomType deadlock.
- Integration points (exact): `ReservationsService.update()` now transactional — computes `diffEntitlements` (status unchanged, so consuming flag same both sides) and calls `applyDelta` in the same tx as the reservation save (handles date extend/shorten/shift + roomType change via PATCH). `ReservationWorkflowService.extendStay()` computes the added-nights diff and calls `applyDelta`. `moveRoom` untouched (roomId change = zero inventory effect by construction).
- Verified: build clean; full `npm test` 523/523 (56 suites) — added diffEntitlements matrix (extend/shorten/shift/roomType-transfer/non-consuming/unchanged), applyDelta engine tests (atomic transfer + global lock order + rollback-no-partial + date shift), workflow release/reserve assertions (PENDING/CONFIRMED->CANCELLED, CONFIRMED->NO_SHOW, CHECKED_IN->CHECKED_OUT release once; double-release blocked; extend reserves added-only; moveRoom no inventory). Live DB e2e (throwaway, cleaned): 8/8 PASS incl. cancel/no-show release, double-release guard, extend/shorten/shift, roomType transfer, and unavailable-target full rollback (reservation dates + inventory unchanged). 0 residue, 0 invariant violations, app boots.
- Before/after (roomType transfer, rtA->rtB, nights d1,d2): rtA `d1:1 d2:1` + rtB `d1:0 d2:0` → after `rtA d1:0 d2:0` + `rtB d1:1 d2:1`. Unavailable-target extend: night full → whole PATCH rejected, reservation departure + all inventory unchanged.
- Phase-1C-a4 (NOT started): true concurrent last-room oversell tests + full reconciliation/regression gate.

## 2026-08-16 — Phase 1C-a4: Concurrency proof + reconciliation gate + final hardening (1C-a COMPLETE)
- DB-backed concurrency spec `availability.concurrency.spec.ts` (boots real AppModule + Postgres, isolated far-future fixtures, self-cleaning). Proven with REAL simultaneous transactions: (1) two reserves for the last room -> exactly 1 success + 1 INVENTORY_UNAVAILABLE; (2) two multi-night reserves cannot partially consume the overlapping night; (3) lazy row creation under concurrency converges to ONE row, never oversells; (4) concurrent date-extension for the final target night -> exactly one wins; (5) opposite-direction roomType transfers do NOT deadlock (global roomType-asc,date-asc lock order) and conserve counters; (6) reserve/restore batches never duplicate and stay within [0,capacity]; (7) two simultaneous reservation creates never oversell (sold == consuming reservation count); (8) create-vs-cancel race leaves sold == consuming count.
- Reconciliation gate: spec captures global baseline before scenarios, and after full cleanup asserts countsByType returns EXACTLY to baseline -> 1C-a adds zero net drift.
- FINAL HARDENING (fixes the one gap the testing agent flagged): added `reservations.inventory_reserved` boolean (migration `20260820090000`, backfilled true only for consuming reservations FULLY represented in the ledger; oversold/skipped stay false). `releaseInventoryEntitlement` now restores ONLY when `wasReserved` -> release is idempotent/paired; cancelling a never-reserved (legacy/backfill-skipped) reservation can no longer phantom-decrement sold. create() sets the flag on reserve; terminate()/checkOut() capture wasReserved then clear the flag in the same save; update()/extendStay() only mutate inventory when the reservation holds an entitlement.
- Read-only drift health-check endpoint: `GET /api/v1/properties/:propertyId/inventory/reconciliation` (OperationsView; never mutates — proven read-only). ReconciliationResult now includes `countsByType`.
- Results: build clean; full `npm test` 533/533 (57 suites) incl. 9 DB-backed concurrency cases + phantom-release guard test; migration applied (287 consuming reservations flagged reserved, 6 legacy flagged false); 0 invariant violations; testing_agent iteration_11 17/17 externally observable cases PASS (overbooking 409 INVENTORY_UNAVAILABLE, release-on-cancel, last-unit concurrency 1x201+4x409, read-only reconciliation, auth 401). Reconciliation before/after: total drift stays 8 discrepancies, ALL confined to the known demo-oversold dates 2026-11-02/03 (Deluxe+Suite) — zero drift on any other date (composition shifted MISSING_ROW->SOLD_MISMATCH only due to the testing agent's own pytest poking those demo dates, not 1C-a code).
- REMAINING RISK: the ~218 pre-existing demo reservations oversold on 2026-11-02/03 remain (known baseline; NOT a 1C-a defect). Recommend a one-off demo-data cleanup + restoring some rooms to READY (8 legacy lifecycle assignment tests error only because the demo property currently has 0 READY rooms).
- **Phase 1C-a is COMPLETE** (authoritative inventory ledger + transactional consume/release/mutate, concurrency-safe, reconciliation-gated, hardened). Next: Phase 1C-b (Rate Plans) — NOT started. Availability calendar API intentionally NOT exposed yet.

## 2026-08-17 — Demo Data Reset (data/environment cleanup only; NO code/architecture change)
- Root of the known baseline drift: a single stale stress-test batch (reservation_code prefix HS260816, all arrival 2026-11-02 -> departure 2026-11-04, created 2026-08-16) = 436 reservations (224 consuming: 218 Deluxe cap-19 + 6 Suite cap-5, physically impossible; + 212 already terminal). No folios attached.
- Actions (single SQL transaction): deleted the 436 junk reservations + 408 audit_events + 274 activity_events referencing them; deleted 2 test-derived room_type_inventory rows (Deluxe 2026-11-02 & 2026-11-03, sold=0); restored 20 rooms (19 NEEDS_CLEANING + 1 INSPECTION) to READY (cleared reason/note). Left OCCUPIED(2)/MAINTENANCE(1)/OUT_OF_SERVICE(1) as intentional realistic states.
- Environment recovery (container had restarted): brought the standalone Postgres cluster at /app/postgres_data back up — note it is owned by uid 103 (Debian-exim), so start with `sudo -u '#103' /usr/lib/postgresql/15/bin/pg_ctl -D /app/postgres_data -l /tmp/pg103.log -o "-p 5432 -k /tmp" start`; then restart the API via `node dist/src/main.js` on :3001.
- Reconciliation BEFORE: 8 discrepancies {MISSING_ROW:2, SOLD_MISMATCH:2, OVERSELL:4}. AFTER: **0 discrepancies (fully consistent)**, 0 invariant violations. Remaining 69 consuming reservations all inventory_reserved=true and fully ledger-represented; 35 far-future seed reservations are all terminal (31 CANCELLED + 4 NO_SHOW), hold no inventory.
- Verified: build clean; full Jest 533/533 (57 suites); live assign-room lifecycle now works against READY rooms (previously errored on 0 READY rooms). NO 1C-a inventory architecture, entitlement rules, locking, lifecycle logic, migrations, or production code changed.

## 2026-08-17 — Phase 1C-b1: Rate Plan model completion + CRUD (pricing only; inventory untouched)
- Reused existing infra (NOT duplicated): `rate_plans`, `room_type_daily_rates` (date-wise overrides), `guest_pricing_policies`/`child_age_bands`+`ChildPricingService` (extra-child), `property_policies.rate_plan_id`+`PolicyResolverService` (policy overrides), money = numeric(12,2) decimal STRING.
- Migration `20260821090000`: added `rate_plans.meal_plan` (enum ROOM_ONLY/BREAKFAST/HALF_BOARD/FULL_BOARD, default ROOM_ONLY) + `rate_plans.refundable` boolean default true; new `rate_plan_room_types` table (unique (rate_plan_id, room_type_id); FKs property RESTRICT / rate_plan CASCADE / room_type RESTRICT; base_occupancy>=1, base_rate>=0, extra_adult_charge>=0, extra_child_charge>=0) = applicable room types + base commercial terms per room type.
- New entity `RatePlanRoomTypeEntity`; `MealPlan` enum; extended `RatePlanEntity` (mealPlan, refundable). Service methods added: `updateRatePlan`, `findRatePlanRoomTypes`, `upsertRatePlanRoomType`, `removeRatePlanRoomType`, `findDailyRates`, `removeDailyRate` (existing createRatePlan/findRatePlans/findRatePlan/createDailyRate reused).
- HTTP CRUD wired on `RatesController` under `/api/v1/properties/:propertyId/rates`: rate-plans (GET list / POST / GET / PATCH), rate-plans/:id/room-types (GET / PUT upsert / DELETE :roomTypeId), rate-plans/:id/daily-rates (GET / POST), daily-rates/:id (DELETE). Permissions: SettingsView/BookingsView to read, SettingsManage to write. DTOs with money-string + occupancy(int>=1) + enum validation.
- Verified: build clean; rates suite 78/78; full Jest 538/538 (57 suites); live CRUD smoke (create plan w/ mealPlan+refundable, upsert base pricing incl. extra charges, daily override, list, invalid baseRate -> 400, deactivate -> INACTIVE); inventory reconciliation still consistent + 0 invariant violations (zero inventory code touched).
- NOT started: 1C-b2 (RateResolverService) and 1C-b3 (reservation ratePlan link + commercial snapshot). Stopped for verification as instructed.

## 2026-08-17 — Phase 1C-b2: RateResolverService (centralized, deterministic, read-only)
- Schema confirmations: `room_type_daily_rates` is uniquely scoped by (roomTypeId, ratePlanId, stayDate) with a rate_plan_id FK — overrides are already per-rate-plan (isolated across plans); no correction needed. `rate_plan_room_types.base_occupancy` is integer with CHECK>=1 + @IsInt()@Min(1) (whole-number, not monetary); money stays numeric(12,2) decimal strings.
- New `RateResolverService` (`core/rates/rate-resolver.service.ts`), registered + exported in RatesModule (added PoliciesModule import). READ-ONLY: no writes to reservations/inventory/rooms/folios/taxes/invoices/payments.
- Resolution precedence per stay night (independent per night): (1) validate rate plan exists+ACTIVE for property; (2) validate roomType applicability via rate_plan_room_types (carries base rate); (3) per night: DAILY_OVERRIDE (room_type_daily_rates for property+ratePlan+roomType+date) > BASE_RATE (applicability.baseRate); (4) extra-adult = max(0, adults-baseOccupancy) * extraAdultCharge per night; (5) child charges delegated to existing ChildPricingService per night (nights=1, that night's room rate) — no duplication of age-band logic; (6) effective policies via existing PolicyResolverService (RATE_PLAN override > PROPERTY) for CANCELLATION/NO_SHOW/INDIVIDUAL_DEPOSIT/GROUP_DEPOSIT/EARLY_CHECK_IN/LATE_CHECKOUT; (7) returns mealPlan/refundable + per-night breakdown.
- Decimal-safe: all arithmetic in integer cents; rounding (Math.round, half-up) applied only at the per-line monetary boundary; grand total = sum of cents (no re-round drift). Explicit failure (NotFound/BadRequest) when no valid price resolves — never substitutes 0.
- Verified: build clean; focused rates+policies suites 115/115 (8 suites) incl. 14 new resolver tests (override precedence, base fallback, cross-plan isolation, base-occupancy no-charge, extra-adult, child integration, inactive/missing plan, missing base/override, policy RATE_PLAN vs PROPERTY, multi-night mixed, decimal precision). Full Jest regression deferred to the 1C-b completion gate per instruction.
- Assumptions: resolver requires an explicit ratePlanId (property-default fallback is a 1C-b3 create-time concern); applicability row is required and is the base-rate source (roomType not on plan => explicit failure). NOT started: 1C-b3.

## 2026-08-17 — Phase 1C-b3 VERIFIED: Reservation rate-plan link + immutable commercial snapshot
- Scope confirmed live: `reservations.rate_plan_id` + `reservations.rate_snapshot` (jsonb). `ReservationPricingService.buildCommercialSnapshot()` wired into create() (only when status===CONFIRMED) and confirm() (freezes only if snapshot absent). Pricing is fully isolated from inventory (never touches inventory_reserved/sold/locking/rooms).
- Snapshot shape (v1): {version, pricingStatus PRICED|UNPRICED, snapshotAt, ratePlan{id,code}, roomTypeId, mealPlan, refundable, occupancy{adults,children,baseOccupancy,extraAdults}, nights[{date,source BASE_RATE|DAILY_OVERRIDE,roomRate,extraAdultCharge,childCharge,nightTotal}], totals{room,extraAdult,child,grandTotal}, policies{<policyType>:{policy,source PROPERTY|RATE_PLAN,isActive}}, childPricing{limitations}}. UNPRICED => {reason 'NO_APPLICABLE_RATE_PLAN', ratePlanId null, NO fabricated totals}.
- OBSERVABILITY: ratePlanId/rateSnapshot are intentionally hidden by ReservationsMapper (absent on GET /reservations and /reservations/:id). They are ONLY exposed via GET /api/v1/properties/:propertyId/stays/:reservationId -> data.reservation. (UI/consumers must read from the stays endpoint.)
- ENV NOTE: the StayOS NestJS API runs on port 3001 (node dist/src/main.js, started from /entrypoint context as an unsupervised orphan — NO auto-restart, NO hot reload). The external preview URL 502s (k8s ingress expects 8001). Test against http://localhost:3001/api/v1. On this fork the live process was a STALE build predating 1C-b3; fixed via `npm run build` (tsc clean) + kill + `nohup node dist/src/main.js`. If code changes, REBUILD + restart manually (hot reload is NOT active).
- VERIFICATION (this session): focused Jest specs 151/151 (rates.service, rates.controller, rate-resolver, reservation-pricing, reservations.service, reservation-workflow, policy-resolver). Backend testing_agent iteration_12: 27/27 PASS externally-observable 1C-b flows (CRUD+validation, applicability, daily overrides, PRICED math 13000.00, DAILY_OVERRIDE vs BASE_RATE, default-plan fallback, UNPRICED, PENDING->confirm freeze, snapshot immutability, transactional rollback on inapplicable/INACTIVE/unknown plan). Inventory reconciliation AFTER = consistent (0 discrepancies, 0 invariant violations) => 1C-a untouched.
- OPEN DESIGN DECISIONS (NOT bugs; deferred for user): (1) `rate_plan_room_types.extra_child_charge` is persisted but IGNORED by RateResolverService — child charges come solely from ChildPricingService/property guest-pricing policy; with no property policy seeded, snapshot child totals = 0.00. Decide: wire extraChildCharge into resolver OR deprecate the column. (2) Post-CONFIRMED occupancy/date edits do NOT re-price (snapshot stays immutable by design => can go stale). Decide a re-price/snapshot-version strategy OR block commercial-impacting edits before billing (1D) consumes the snapshot.
- DEMO DATA: testing_agent created QA rate plans (codes QA*/SMOKEBAR) + far-future reservations; main agent cleaned up afterward — all 16 today-created reservations CANCELLED (0 hold inventory), all rate plans set INACTIVE with applicability removed and isDefault cleared (0 ACTIVE, 0 DEFAULT). NOTE: no DELETE endpoint for rate plans, so INACTIVE QA rows linger harmlessly. Property is back to effectively zero active rate plans (its pre-test state). Run bootstrap:demo only if pristine rate rows are required.
- **Phase 1C-b (Rate Plans + commercial snapshot) is COMPLETE & VERIFIED.** Next (awaiting explicit user approval, plan-first): Phase 1C-c Restrictions (stopSell/minStay/maxStay/CTA/CTD). NOT started.

## 2026-08-17 — Phase 1C-b Gap Fix A: Child Pricing (authoritative single-mechanism) — DONE & VERIFIED
- Authoritative rule implemented: a child resolves to exactly ONE active age band; that band's pricingMode is the SINGLE source of the child's per-night amount. No child is priced by more than one mechanism (no double-charge).
- New `ChildPricingMode.RATE_PLAN_EXTRA_CHILD` (migration `20260823090000`, additive `ALTER TYPE ... ADD VALUE` via DO-block, backward compatible): a band with this mode is priced from the applicable `rate_plan_room_types.extra_child_charge` per night — this is how rate-plan-specific child pricing is expressed (the previously-dead column is now wired via the resolver, NOT a second engine).
- ADULT_PRICING band + over-max-age children now correctly billed as extra adults via the rate plan's `extraAdultCharge` (fixes the old "no adult mechanism" limitation). Unified occupancy: extraOccupantsCharged = max(0, adults + adultPricedChildren - baseOccupancy); adults fill base occupancy first, adult-priced children beyond base are charged (source EXTRA_ADULT), those absorbed by base occupancy cost 0 (source BASE_OCCUPANCY_ABSORBED). Their charge lives in the extra-adult bucket only — never the child bucket.
- Snapshot now carries auditable per-child lines: `rateSnapshot.childPricing.lines[] = {age, category, source, amount}` (sources: BAND_FREE / BAND_FIXED_PER_NIGHT / BAND_PERCENT_OF_ROOM_RATE / RATE_PLAN_EXTRA_CHILD / EXTRA_ADULT / BASE_OCCUPANCY_ABSORBED / ABOVE_MAXIMUM_CHILD_AGE). occupancy gains adultPricedChildren + extraOccupantsCharged.
- Files: child-pricing-mode.enum.ts, child-pricing.service.ts (resolveChildPricing gains extraChildChargePerNight; bandSource; RATE_PLAN_EXTRA_CHILD branch; removed stale limitation), rate-resolver.service.ts (per-child accumulation, unified extra-occupant math, buildChildPricingLines, ResolvedRate.occupancy + childPricing.lines), rates.service.ts (guest-pricing-policy upsert accepts RATE_PLAN_EXTRA_CHILD, rejects it defining fixedAmount/percentage). reservation-pricing.service forwards resolved.childPricing/occupancy unchanged.
- Verified: focused specs 85/85 (child-pricing, rate-resolver, rates.service, reservation-pricing); tsc --noEmit clean; migration applied; rebuilt + restarted live API; live E2E smoke (self-cleaned): ages [4,8,14] w/ FREE/RATE_PLAN_EXTRA_CHILD/ADULT_PRICING bands + extraChildCharge 700 + extraAdultCharge 1500 over 2 nights => child 1400.00 (age 8), extraAdult 3000.00 (age 14 as EXTRA_ADULT, not double-charged), room 10000.00, grandTotal 14400.00; per-child audit lines correct. Full Jest suite intentionally NOT run (testing-budget rule).
- NOTE: no rate-plan/reservation snapshot versioning yet — Gap Fix B (dedicated append-only reservation_rate_snapshots) is APPROVED but implemented only after explicit go-ahead. Not started 1C-c/1D/POS/frontend.

## 2026-08-17 — Phase 1C-b Gap Fix B: Immutable versioned reservation commercial snapshots — DONE & VERIFIED
- New append-only table `reservation_rate_snapshots` (migration `20260824090000`): {reservation_id FK CASCADE, property_id, version, status ACTIVE|SUPERSEDED, trigger (INITIAL/RATE_PLAN_CHANGE/DATE_CHANGE/ROOM_TYPE_CHANGE/OCCUPANCY_CHANGE/STAY_EXTENSION/AMENDMENT), reason, rate_plan_id, commercial_input_hash md5, snapshot jsonb, effective_from, superseded_at, superseded_by_version, created_at}. Partial unique index `UQ_rrs_one_active_per_reservation (reservation_id) WHERE status='ACTIVE'` enforces exactly one ACTIVE version; unique (reservation_id, version).
- `reservations.rate_snapshot_version` added. `reservations.rate_snapshot` + `rate_plan_id` remain the backward-compatible MIRROR of the ACTIVE version (stays endpoint + all current readers unchanged). Invariant: reservation.rate_plan_id == ACTIVE row's rate_plan_id (== ratePlanId in its commercial hash) — ratePlanId never diverges.
- New `ReservationRateSnapshotService` (services/reservation-rate-snapshot.service.ts): `computeCommercialHash` (normalized md5 of ratePlanId|roomTypeId|arrival|departure|adults|sorted childAges), `recordInitialVersion` (idempotent v1 INITIAL), `amend` (no-op detection via hash; else supersede ACTIVE + insert new immutable version + update mirror). All writes take the caller's EntityManager => atomic with the reservation amendment + inventory delta.
- Wiring: create()-CONFIRMED and confirm() => recordInitialVersion v1 (inside txn; bad rate plan rolls back create). update() classifies commercial vs operational via hash; commercial change on PENDING/CONFIRMED with an existing ACTIVE snapshot => amend() new version (no-op & operational edits => no version); status gate: CHECKED_IN rejected in update() (extension only), CANCELLED/NO_SHOW/CHECKED_OUT rejected. PENDING with no snapshot stays deferred (confirm creates v1).
- extendStay() FIXED: removed the ad-hoc folio ROOM charge + hard-coded 3500 nightly fallback + paymentStatus recompute (`postExtensionCharge` deleted). Extension now re-prices the full stay through the authoritative resolver and records a STAY_EXTENSION version (`postExtensionSnapshotVersion`). Inventory delta for added nights unchanged (1C-a preserved). BILLING HANDSHAKE / remaining for 1D: extension (and any commercial amendment) no longer auto-posts folio charges; Phase 1D must read the ACTIVE snapshot version (and its history) to (re)generate folio/invoice lines and recompute paymentStatus. No second billing mechanism remains in workflow.
- Backfill: 18 existing snapshotted reservations => 18 v1 ACTIVE rows, no recompute; rate_snapshot_version=1; 0 invariant violations.
- Files (new): domain/reservation-rate-snapshot-status.enum.ts, domain/reservation-rate-snapshot-trigger.enum.ts, infrastructure/reservation-rate-snapshot.entity.ts, services/reservation-rate-snapshot.service.ts(+spec), migration. (modified): reservation.entity.ts (rate_snapshot_version optional), reservations.module.ts, reservations.service.ts (create/update + gates + trigger), reservation-workflow.service.ts (confirm/extend), + specs.
- Verified (focused only, full Jest NOT run): 155/155 focused specs (child-pricing, rate-resolver, rates.service, reservation-rate-snapshot, reservation-pricing, reservations.service, reservation-workflow); tsc --noEmit clean; migration applied; rebuilt+restarted live API. Live E2E (self-cleaned): initial v1 (10000) -> DATE_CHANGE v2 (15000) -> RATE_PLAN_CHANGE v3 (18000, ratePlanId follows) -> roomType+plan AMENDMENT v4 (24000) -> OCCUPANCY_CHANGE v5 (30000); operational edit & commercial no-op made NO version; failed re-price (inapplicable plan) => 400 with version+mirror unchanged (rollback); CANCELLED commercial amend => 400; CHECKED_IN extension => new version via resolver (10000->20000), general commercial amend on CHECKED_IN => 400. DB: exactly one ACTIVE, immutable superseded chain, mirror (ratePlan+snapshot) matches ACTIVE, 0 global one-active violations.
- **Phase 1C-b (Rate Plans + child pricing + versioned commercial snapshots) is COMPLETE.** Not started: 1C-c, 1D, POS, frontend.

## 2026-08-17 — Phase 1C-c1: Restrictions foundation (schema + engine + CRUD) — DONE & VERIFIED
- New table `rate_restrictions` (migration `20260825090000`, additive/no-backfill): {property_id, room_type_id, rate_plan_id NULL=roomType-level baseline, date, stop_sell/cta/ctd nullable bool (NULL=inherit), min_stay/max_stay nullable int, timestamps}. Partial unique indexes: one roomType-level row + one row per rate plan per date. FKs to room_types/rate_plans (CASCADE). Per-row CHECKs (min/max>=1, max>=min). Added ApiErrorCode.RESTRICTION_VIOLATION.
- `RestrictionService` (RatesModule, exported, READ-ONLY — never touches inventory/pricing/snapshots): `evaluateStay({propertyId,roomTypeId,ratePlanId?,arrivalDate,departureDate})` -> {sellable, violations[]}; `assertStaySellable()` throws 422 RESTRICTION_VIOLATION with ALL violations (type, date?, requiredMinStay/allowedMaxStay). CRUD: bulk `upsertRestrictions` (inclusive date range; only provided fields written, others preserved), `listRestrictions`, `deleteRestriction`.
- SEMANTICS (approved): occupied nights = [arrival..departure-1]. stopSell blocks any occupied night; CTA only on arrival date; CTD only on departure date; minStay/maxStay = arrival-anchored LOS (LOS=departure-arrival nights). Precedence = specificity override, NULL=inherit: effective = ratePlanRow.field ?? roomTypeRow.field ?? default(bool false / int null); an explicit rate-plan `false` reopens a baseline restriction.
- CRUD endpoints under rates: GET/PUT `/properties/:propertyId/rates/restrictions`, DELETE `/properties/:propertyId/rates/restrictions/:id` (SettingsView/BookingsView read, SettingsManage write).
- Verified: focused specs 15/15 (restriction.service.spec evaluation matrix + 422 payload; rates.controller.spec); tsc clean; migration applied; rebuilt+restarted live API; live CRUD smoke (bulk upsert 3 dates, list, merge-preserving partial re-upsert, delete cleanup -> 0). Full Jest/reservation/inventory suites intentionally NOT run.
- NOT wired into reservation lifecycle yet (1C-c2/c3). `assertStaySellable` ready for wiring.

### APPROVED change-aware amendment validation rules — TO IMPLEMENT in 1C-c2/c3 (do NOT replace with blanket full-stay revalidation)
Principle: restrictions gate newly-introduced/re-scoped sale entitlement, NOT already-held grandfathered entitlement. Existing confirmed reservations are grandfathered.
- create() (1C-c2): validate the full requested stay under (roomType, ratePlan). PENDING & CONFIRMED are sales -> validate both.
- confirm(): NO restriction re-validation (PENDING already a held sale).
- update()/extend (1C-c3), change-aware:
  * occupancy/guest-count-only (roomType/ratePlan/dates unchanged) -> NO stay-restriction check.
  * date extension -> validate only newly-added occupied nights for stopSell + new departure CTD + resulting LOS min/maxStay; CTA only if arrival changed.
  * date shortening -> validate resulting LOS + new departure CTD; do NOT reject on unchanged historical nights now stop-sold.
  * arrival/date shift -> validate newly-introduced nights + CTA on new arrival + CTD on new departure + LOS min/maxStay.
  * roomType change -> validate the COMPLETE resulting stay (entire entitlement newly introduced under another roomType).
  * ratePlan change -> validate the COMPLETE resulting stay under the new plan (restriction scope changed).
  * operational-only edits -> NO restriction validation.
- Error: 422 RESTRICTION_VIOLATION with all violations.
- Staff force/override: DEFERRED (must later be permission-controlled + audited, not generic force=true).

## 2026-08-17 — Phase 1C-c2: Restriction validation on reservation CREATE — DONE & VERIFIED
- Wired `RestrictionService.assertStaySellable()` into `ReservationsService.create()` for new PENDING and direct-CONFIRMED reservations. Validation is INSIDE the create transaction, BEFORE any write/inventory/snapshot => a RESTRICTION_VIOLATION rolls back the whole create (no reservation, no inventory, no snapshot/version).
- Validation ORDER in create(): (pre-txn) validateReferences + status/willReserve + reservationCode. (txn) 1) resolveEffectiveRatePlanId, 2) assertStaySellable(effective plan) [throws 422 -> rollback], 3) save reservation, 4) if CONFIRMED recordInitialVersion (snapshot v1), 5) reserve inventory. Full requested stay validated (creation introduces the whole entitlement).
- EFFECTIVE rate plan (single authoritative resolution, no duplication): extracted `ReservationPricingService.resolveEffectiveRatePlanId({propertyId,roomTypeId,ratePlanId})` (explicit applicable plan -> property default applicable plan -> null=roomType baseline), reused by BOTH buildCommercialSnapshot and restriction scoping. A missing request ratePlanId therefore evaluates the DEFAULT plan's restrictions, never bypasses them.
- confirm() remains EXEMPT (a held PENDING must not fail confirmation due to restrictions added after the hold). Verified live.
- Error contract: 422 RESTRICTION_VIOLATION. Violations surfaced through the standard error envelope `error.details[]` (field=type, message, rejectedValue={date, requiredMinStay, allowedMaxStay}); raw `violations[]` also included on the thrown HttpException body. (Note: the global HttpExceptionFilter only passes code/message/details, so violations are mapped into details.)
- Files: reservation-pricing.service.ts (resolveEffectiveRatePlanId extracted+reused), reservations.service.ts (create() gate + RestrictionService injected), reservations.module.ts already imports RatesModule (exports RestrictionService), restriction.service.ts (assertStaySellable -> details), + specs.
- Verified (focused only): 36/36 focused specs (restriction.service.spec + reservations.service.spec incl. clear PENDING/CONFIRMED succeed, 422 propagation + NO side effects, explicit/default/baseline effective-plan matrix); tsc clean; migration n/a (c1 table). Live E2E (self-cleaned): default-plan stopSell blocks create w/o ratePlanId (422) + rollback (no reservation persisted); explicit-plan stopSell; CTA@arrival; CTD@departure; minStay; maxStay; multiple violations returned together in details; roomType baseline applies when no plan/default; clear PENDING & CONFIRMED succeed; confirm() unaffected by a restriction added after the PENDING hold. Full Jest/inventory suites NOT run.
- NOTE (build race learned): the local API runs a compiled dist orphan with NO hot reload — ALWAYS run `npm run build` to completion (foreground) BEFORE killing/restarting `node dist/src/main.js`, else a stale build serves and validation appears not to fire.
- NOT started: 1C-c3 (update()/extendStay change-aware validation — rules already documented in the 1C-c1 section), 1C-d, 1D.


## 2026-08-17 — Phase 1C-c3: Change-aware restriction validation on AMENDMENTS — DONE & VERIFIED
- Wired `RestrictionService.assertAmendmentSellable(current, next)` (grandfathering engine, already in restriction.service.ts) into BOTH `ReservationsService.update()` and `ReservationWorkflowService.extendStay()`, inside their existing transactions BEFORE any inventory delta / snapshot amend => a 422 RESTRICTION_VIOLATION rolls back reservation dates + inventory + snapshot version together.
- update(): restriction gate now runs on ANY commercial change (dates/roomType/ratePlan/occupancy hash diff) for sellable statuses, DECOUPLED from snapshot versioning. **GAP FIX (approved):** previously the gate only fired when `reservation.rateSnapshotVersion != null`, so a PENDING amendment (no snapshot until confirm) skipped restriction validation — now PENDING amendments are gated too. The snapshot `amend()` is still gated by `rateSnapshotVersion != null` (PENDING stays deferred to confirm). CHECKED_IN/terminal commercial edits are still rejected upstream by `assertCommercialAmendmentAllowed` (extension is the only CHECKED_IN commercial path).
- Symmetry fix: BOTH current and next scopes now resolve the EFFECTIVE rate plan (explicit -> default -> baseline) before the amendment evaluation, so a null ratePlanId that resolves to the same default plan is NOT a false scope change (avoids spurious full-stay revalidation that would reject grandfathered nights).
- Change-aware semantics (via evaluateAmendment): scope change (roomType OR effective ratePlan) => validate the FULL resulting stay; same scope + date change => validate only newly-added occupied nights (stopSell), CTA only if arrival changed, CTD only if departure changed, LOS min/maxStay. extendStay() (CHECKED_IN) passes current departure vs new departure => added nights + new-departure CTD + resulting LOS only; arrival unchanged so CTA not re-checked; grandfathered nights untouched.
- Files: reservations.service.ts (update(): decoupled restriction gate + currentEffectiveRatePlanId), reservation-workflow.service.ts (extendStay gate — pre-existing), + specs (reservations.service.spec: PENDING-amendment gate test; reservation-workflow.service.spec: extendStay gate + rollback tests).
- Verified (focused only, full Jest NOT run per testing-budget rule): 85/85 focused specs (restriction.service.spec, reservations.service.spec, reservation-workflow.service.spec); nest build clean; rebuilt + restarted live API (port 3001). Live E2E (self-cleaning python /tmp/verify_c3.py, isolated per-scenario date weeks): 12/12 PASS — 1a grandfathered historical stopSell does NOT block extension; 1b added-night stopSell blocks + full rollback (departure, snapshot version, added-night inventory all unchanged); 2 CTD on new departure blocks; 3 maxStay blocks longer LOS; 4 roomType change revalidates full stay + rollback; 5 operational-only edit exempt. 0 residue, 0 inventory invariant violations after cleanup.
- **Phase 1C-c (Restrictions foundation: schema/engine/CRUD + create validation + change-aware amendment validation) is COMPLETE.** Deferred: staff force/override (permission-controlled + audited — NOT generic force=true). NOT started: 1C-d (source expansion PHONE/CHANNEL/OTHER + external IDs), 1D (folio/billing/tax/invoice), POS, frontend.


## 2026-08-17 — Phase 1C-d1: Reservation source canonicalization + provenance immutability — DONE & VERIFIED
- APPROVED 1C-d sub-phase sequence: d1 (this) = enum canonicalization + default + source immutability; d2 = external metadata columns (external_reservation_id/external_confirmation_id/source_provider) + partial-unique index + TRUE-idempotent create (return existing on same external identity + materially-equivalent data; 409 EXTERNAL_RESERVATION_CONFLICT on conflicting data; never a 2nd row) + sourceProvider REQUIRED when externalReservationId present; d3 = list filtering by source/provider + write-once externalConfirmationId (NULL->value once, then immutable).
- Enum: additively added FRONT_DESK, PHONE, CHANNEL, OTHER to reservations_source_enum via guarded ALTER TYPE ADD VALUE (migration 20260826090000). Legacy values DIRECT, WALK_IN, OTA, WEBSITE, CORPORATE PRESERVED untouched — NO backfill (historical origin ambiguous; DIRECT != necessarily FRONT_DESK, OTA != necessarily CHANNEL). Canonical vocabulary for new writes: FRONT_DESK/WALK_IN/PHONE/WEBSITE/CORPORATE/CHANNEL/OTHER; DIRECT+OTA marked @deprecated legacy (reads only). DB enum now: DIRECT,WALK_IN,OTA,CORPORATE,WEBSITE,FRONT_DESK,PHONE,CHANNEL,OTHER.
- Default: create() now defaults source to FRONT_DESK (in toCreatePersistenceFields, `dto.source ?? FRONT_DESK`) — DB column default left as DIRECT to avoid the Postgres "use new enum value in same tx as ADD VALUE" pitfall; app always sets source so the column default never fires. Create DTO ApiPropertyOptional default annotation updated to FRONT_DESK.
- Immutability: `source` removed from UpdateReservationDto (OmitType ['status','source']). Provenance is set-once at create. Because the global ValidationPipe uses forbidNonWhitelisted:true, a PATCH containing `source` now returns 400 (same established pattern as the already-immutable `status`) — stronger, consistent enforcement, not a silent ignore. No source bypasses restrictions/inventory/pricing/snapshots/lifecycle (create() path unchanged — all 1C-a/b/c gates intact).
- Files: domain/reservation-source.enum.ts (canonical + legacy), migrations/20260826090000-AddCanonicalReservationSources.ts, dto/create-reservation.dto.ts (default annotation), dto/update-reservation.dto.ts (omit source), reservations.service.ts (import + FRONT_DESK default), reservations.service.spec.ts (+1 test).
- Verified: tsc --noEmit clean; nest build clean; migration applied (enum verified in DB); focused reservations.service.spec 31/31 (incl. new default-FRONT_DESK + explicit-CHANNEL test); rebuilt+restarted live API (:3001). Live E2E (self-cleaned): omit->FRONT_DESK, explicit CHANNEL accepted, PATCH{source} -> 400, notes-only PATCH -> 200 with source unchanged. 0 residue. Full Jest NOT run (testing-budget rule).
- NOT started: 1C-d2 (external IDs + idempotency), 1C-d3 (filtering + externalConfirmationId write-once). Legacy DIRECT/OTA still valid inputs (accepted, not rejected) for backward compat.


## 2026-08-17 — Phase 1C-d2: External booking identity + true idempotent create — DONE & VERIFIED
- Schema (migration 20260827090000, additive/backward-compatible, all nullable): reservations.source_provider varchar(64) (normalized: trimmed + UPPERCASED), external_reservation_id varchar(128), external_confirmation_id varchar(128). PARTIAL UNIQUE INDEX UQ_reservations_external_identity ON (property_id, source_provider, external_reservation_id) WHERE external_reservation_id IS NOT NULL — property+provider scoped, NEVER global. Added ApiErrorCode.EXTERNAL_RESERVATION_CONFLICT.
- DTO: create accepts sourceProvider (@Transform trim+uppercase, @Length 1-64), externalReservationId, externalConfirmationId; sourceProvider REQUIRED when externalReservationId present (@ValidateIf + service-side defensive guard -> 400). All three exposed via ReservationsMapper + ReservationResponseDto. IMMUTABLE after create: UpdateReservationDto now OmitType ['status','source','sourceProvider','externalReservationId','externalConfirmationId'] (PATCH with any of them -> 400 via forbidNonWhitelisted). externalConfirmationId controlled write-once setter deferred to 1C-d3.
- Material equivalence (idempotency contract) compares ONLY the external booking fields: roomTypeId, arrivalDate, departureDate, guestId, adults, children, childAges(sorted), source, sourceProvider, and ratePlanId WHEN the retry supplies it. EXCLUDES operational fields (roomId, notes, specialRequests, paymentStatus, externalConfirmationId).
- create() idempotent flow: (fast path, pre-txn) if externalReservationId present -> findByExternalIdentity(property, provider, extId); if found -> equivalent => return existing (NO reservation/inventory/snapshot/event side effects, transaction never opened), conflicting => 409 EXTERNAL_RESERVATION_CONFLICT. If none -> normal create path (RestrictionService.assertStaySellable -> snapshot -> AvailabilityService.reserve + locking all UNCHANGED). Concurrency convergence: on ANY unique violation (23505) when the request carried an external identity — external-identity index OR the reservation_code index (concurrent creates compute the same code) — the rolled-back loser re-fetches the committed winner by external identity and resolves idempotently (equivalent => return winner, conflicting => 409). DB unique index is the final protection; no raw duplicate-key surfaced.
- Files: infrastructure/reservation.entity.ts (3 cols + partial unique index decorator), migrations/20260827090000-AddReservationExternalIdentity.ts, common/errors/api-error-code.enum.ts (+EXTERNAL_RESERVATION_CONFLICT), dto/create-reservation.dto.ts (fields + provider-required + normalization), dto/update-reservation.dto.ts (omit external identity), dto/reservation-response.dto.ts + reservations.mapper.ts (expose fields), reservations.service.ts (normalizeProvider, findByExternalIdentity, materialReservationEquivalent, resolveIdempotentRetry, isUniqueViolation, create() pre-check + concurrency recovery + persistence fields), reservations.service.spec.ts (+6 tests).
- Verified: tsc + nest build clean; migration applied (columns + partial index confirmed in DB); focused reservations.service.spec 37/37 (external-identity idempotency: normal create + lookup scoping, provider normalization, provider-required, equivalent-retry no-side-effects, 409 conflict, concurrency convergence). Live E2E /tmp/verify_d2.py 21/21 (real API): identity persisted, equivalent retry returns same row (1 row), conflicting retry 409 (still 1 row), missing provider 400 (0 rows), same extId different provider -> 2 rows allowed, 6 concurrent identical creates -> all 201 same id + exactly 1 row persisted, normal FRONT_DESK create unchanged. 0 residue, 0 inventory invariant violations. Full Jest NOT run (testing-budget rule).
- Concurrency learning: reservation-code generation has a pre-existing race (concurrent creates compute the same HSYYMMDD-NNNNN); for external requests this is now absorbed by the idempotency recovery, but a general fix (sequence/retry) is still open for non-external concurrent creates — out of scope for d2.
- NOT started: 1C-d3 (list filtering by source/provider + externalConfirmationId write-once NULL->value transition). Cross-property allowance is structurally guaranteed (property_id is the leading index column) + covered by the unit lookup-scoping test (no 2nd property seeded to exercise live).


## 2026-08-17 — Reservation-code concurrency hardening (isolated slice before 1C-d3) — DONE & VERIFIED
- Replaced the racy `count()+1` reservation-code generation with an ATOMIC per-property counter. New table `reservation_code_counters (property_id PK -> properties ON DELETE CASCADE, last_value bigint DEFAULT 0, updated_at)` (migration 20260828090000). Allocation = single `INSERT ... ON CONFLICT (property_id) DO UPDATE SET last_value = last_value + 1 RETURNING last_value` — collision-safe for ALL sources (FRONT_DESK/PHONE/non-external included).
- Design: the counter statement runs in its OWN auto-committed statement (this.dataSource.query) BEFORE the main create transaction, so the counter row lock is held ~1ms instead of the whole create => the counter is never a serialization bottleneck. Format preserved: `HS{YYMMDD}-{NNNNN}` (per-property running sequence, creation-date prefix; suffix widens past 99999 gracefully). Rolled-back creates may leave a gap (acceptable; the old scheme also gapped on deletes) but NEVER a duplicate. External idempotency (1C-d2) unchanged — the fast path returns before code allocation; the concurrency-recovery catch still absorbs any unique violation for external requests.
- Backfill: each property's last_value seeded from MAX trailing-numeric suffix of existing codes (Hillston: 732; count was 322 — old fallback codes had high suffixes, so count-based backfill would have collided; max-suffix backfill is safe). New codes continue from 733+.
- Files: migrations/20260828090000-CreateReservationCodeCounters.ts, reservations.service.ts (nextReservationCode via dataSource.query before txn; removed old generateReservationCode + its count/findOne loop + fallback; removed now-unused EntityManager import), reservations.service.spec.ts (dataSource.query counter mock; concurrency test mock adjusted since code-gen no longer calls findOne).
- Verified: tsc + nest build clean; migration applied + backfill confirmed in DB; focused reservations.service.spec 37/37; live real-concurrency E2E /tmp/verify_code_race.py 5/5 — 4 truly-parallel non-external CONFIRMED creates got 4 UNIQUE sequential codes (HS...-00739..00742), 4 distinct ids, exactly 4 rows, self-cleaned to 0, 0 inventory invariant violations. Regression: /tmp/verify_d2.py external-idempotency 21/21 still green. Full Jest NOT run (testing-budget rule).
- **SEPARATE PRE-EXISTING ISSUE FOUND (NOT this slice, flagged):** concurrent creates can exhaust the DB connection pool (default max=10). Root cause: RestrictionService.assertStaySellable + ReservationPricingService.resolveEffectiveRatePlanId run INSIDE the create transaction but on their OWN injected repositories (default pool connections), while the transaction connection sits idle — ~2 connections per concurrent create, so ~N>=5 concurrent creates can deadlock the pool (observed as 'idle in transaction' backends + a Lock wait). This is orthogonal to the code race (existed since 1C-c2 restriction wiring). Live concurrency test kept at N=4 to stay under the pool ceiling. FIX OPTIONS for later: pass the transaction manager into restriction/pricing so they reuse the tx connection, OR raise the pool `extra.max`, OR move restriction/pricing reads before opening the transaction. RECOMMEND addressing before high-concurrency channel ingestion.
- Still NOT started: 1C-d3 (list filtering by source/provider + externalConfirmationId write-once).


## 2026-08-17 — DB connection-pool contention fix (transaction-manager propagation) — DONE & VERIFIED
- Fixes the pre-existing pool-exhaustion flagged in the code-race slice. Root cause: read-only services called INSIDE the create/amend transaction (RestrictionService, ReservationPricingService, RateResolverService, RatesService.findDefaultApplicableRatePlan, PolicyResolverService, ChildPricingService) used their OWN @InjectRepository connections (default pool) while the transaction connection sat idle => ~2+ pool connections per concurrent create => deadlock at concurrency >= ~pool/2 (pool default max=10).
- Fix: each read-only service now accepts an OPTIONAL `manager?: EntityManager` and picks `manager.getRepository(Entity)` when supplied, else the injected repo (standalone callers unchanged). Threaded end-to-end: reservations.create() -> resolveEffectiveRatePlanId(manager) + assertStaySellable(query, manager); recordInitialVersion(manager) -> buildCommercialSnapshot(input, manager) -> resolveEffectiveRatePlanId/findDefaultApplicableRatePlan(manager) + rateResolver.resolve(input, manager) -> childPricingService.resolveChildPricing(..., manager) + resolvePolicies(..., manager) -> policyResolver.resolve(..., manager). Amendment paths also threaded: reservations.update() assertAmendmentSellable(current, next, manager) + amend(manager); workflow.extendStay() assertAmendmentSellable(..., manager) + postExtensionSnapshotVersion->amend(manager); confirm() recordInitialVersion(manager). No manager => identical prior behavior.
- Semantics unchanged: no pricing/restriction/inventory/entitlement changes, NO nested transactions, no correctness-sensitive checks moved out of the transaction. RateResolver.resolvePolicies changed from Promise.all(6) to a SEQUENTIAL loop (a single shared tx connection cannot run concurrent queries) — 6 tiny reads, identical result. reservation-code allocation still its own pre-txn auto-committed counter; external idempotency untouched.
- Files: policy-resolver.service.ts, child-pricing.service.ts, rate-resolver.service.ts (+ sequential resolvePolicies, PropertyPolicyEntity import), rates.service.ts (findDefaultApplicableRatePlan), restriction.service.ts (evaluateStay/assertStaySellable/evaluateAmendment/assertAmendmentSellable), reservation-pricing.service.ts (resolveEffectiveRatePlanId/buildCommercialSnapshot), reservation-rate-snapshot.service.ts (pass manager to pricing in recordInitialVersion + amend), reservations.service.ts (create/update call sites), reservation-workflow.service.ts (extendStay call site). Specs updated for the new trailing manager arg (expect.anything()/undefined): reservations.service.spec, reservation-workflow.service.spec, rate-resolver.service.spec, reservation-pricing.service.spec.
- Verified: tsc + nest build clean; focused specs for ALL touched services 187/187 (policy-resolver, child-pricing, rate-resolver, rates, restriction, reservations.service, reservation-pricing, reservation-rate-snapshot, reservation-workflow). Live E2E /tmp/verify_pool.py 10/10 at concurrency ABOVE pool: Part A 16 parallel creates on distinct nights complete in 0.4s (previously hung >90s) — 16 unique codes + ids + rows, no timeouts; Part B 24 parallel creates on ONE night (cap 19) — sold caps at 19, 19 successes + 5 correct 409s, 0 invariant violations. Regression /tmp/verify_d2.py external-idempotency 21/21 green. 0 residue. Full Jest NOT run (testing-budget rule).
- Pool `extra.max` left at default (env capacity tuning is separate from this architectural fix). Still NOT started: 1C-d3 (list filtering by source/provider + externalConfirmationId write-once).


## 2026-08-17 — Phase 1C-d3: Source/provider filtering + externalConfirmationId write-once — DONE & VERIFIED. **PHASE 1C-d COMPLETE.**
- FILTERING: new ListReservationsQueryDto extends PaginationQueryDto with optional `source` (enum, incl. legacy DIRECT/OTA) + `sourceProvider` (normalized trim+UPPERCASE varchar). Wired into GET /properties/:propertyId/reservations (controller + ReservationsService.findAll). Applied as andWhere on the existing property-scoped query builder AFTER the propertyId where + search, so it composes with search/sort/pagination. Read-only — no inventory/pricing/restriction side effects. Legacy source values remain valid filter inputs (no data touched).
- WRITE-ONCE externalConfirmationId: re-declared on UpdateReservationDto (NOT inherited) with @IsOptional + @ValidateIf(v!==null) + @Transform(trim) + @IsString + @Length(1,128) so an explicit null passes validation and reaches the service for a controlled DOMAIN rejection (empty string still 400 via @Length). Service `resolveExternalConfirmationWriteOnce` (runs BEFORE any work, no side effects): field absent => no-op; existing NULL + value => attach (set inside txn, operational — no restriction/pricing/snapshot/inventory); existing NULL + null => no-op; existing SET + same value => idempotent no-op; existing SET + different/null => throw 409 EXTERNAL_CONFIRMATION_IMMUTABLE (new ApiErrorCode). externalConfirmationId is excluded from toUpdatePersistenceFields (never generic-merged). source/sourceProvider/externalReservationId remain fully omitted from PATCH (400 via forbidNonWhitelisted) — still immutable. externalConfirmationId is NOT part of commercialChanged (computeCommercialHash unchanged) so attaching never triggers amend.
- Preserved (unchanged): inventory locking/entitlement, restriction validation, pricing/rate resolution, commercial snapshots/versioning, lifecycle, 1C-d2 external idempotency, reservation-code counter, transaction/connection-pool (manager propagation) architecture.
- Files: dto/list-reservations-query.dto.ts (new), reservations.controller.ts (use ListReservationsQueryDto), reservations.service.ts (findAll filters + write-once resolver + update() wiring + exclude from persistence fields + import swap), dto/update-reservation.dto.ts (externalConfirmationId re-declared write-once), common/errors/api-error-code.enum.ts (+EXTERNAL_CONFIRMATION_IMMUTABLE), specs: reservations.service.spec (+4 write-once tests), reservations.controller.spec (fixture now includes provenance fields — that spec had been stale since d2 added mapper fields; now fixed).
- Verified: tsc + nest build clean; focused reservations.service.spec 41/41 + reservations.controller.spec 6/6. Live E2E /tmp/verify_d3.py 24/24 on the real API: filter FRONT_DESK/PHONE/CHANNEL, sourceProvider (incl. lowercase-normalized), combined source+provider, legacy DIRECT filterable, property isolation (unknown property 404), pagination-with-filter; write-once attach/idempotent/change-409/clear-409/value-unchanged; source+sourceProvider+externalReservationId PATCH all 400 (immutable) with provenance intact. Regression /tmp/verify_d2.py 21/21 green. 0 residue, 0 inventory invariant violations. Full Jest NOT run (testing-budget rule).
- **PHASE 1C-d (Reservation Source Expansion) COMPLETE:** d1 (canonical enum + default + source immutability), d2 (external identity + idempotent create), reservation-code concurrency hardening, connection-pool contention fix, d3 (filtering + confirmation write-once). Intentionally deferred within 1C: manager restriction override (permission-gated + audited — NOT generic force). Pool `extra.max` env capacity tuning is optional/separate. NOT started (explicitly out of scope): provenance frontend/UI, restrictions calendar/frontend, Phase 1D folio/billing.


## 2026-08-17 — Phase 1D INSPECTION + Phase 1D-a (Folio ledger hardening) — DONE & VERIFIED
### 1D inspection (key finding): a LEGACY billing engine already exists (src/core/billing: FolioEntity/FolioChargeEntity/FolioPaymentEntity + BillingService + TaxService + billing-config). Phase 1D is REMEDIATION+COMPLETION, not greenfield. Critical gaps: (1) room charge uses HARD-CODED nightlyRate=3500 with NO reservation_rate_snapshot link [violates Phase 1C contract — fixed in 1D-b], (2) no amendment reconciliation, (3) racy folio numbering [FIXED in 1D-a], (4) no persisted invoice entity/numbering, (5) no charge void/reversal [FIXED in 1D-a], (6) no refunds/deposits/overpayment model, (7) float money math [FIXED in 1D-a], (8) tax = single flat GST% (no HSN/SAC/split/slab), (9) hidden syncPaidReservationFolio auto-payment coupling [REMOVED in 1D-a].
### Approved 1D sub-phase sequence: 1D-a ledger hardening (DONE) → 1D-b snapshot-driven room charges + reconciliation (reverse+repost, carries rate_snapshot_id/version) → 1D-c Indian GST engine (line-level, HSN/SAC, CGST/SGST/IGST, effective-date/version config, place-of-supply, per-line effective-tax snapshot, cents-safe) → 1D-d payments/refunds/deposits/settlement → 1D-e invoice (property-scoped atomic numbering, FY Apr-Mar reset, immutable FINALIZED + void/credit-note) + checkout integration (balance=0 required). Approved decisions: legacy CLOSED folios untouched/marked legacy; existing OPEN folios reconcile in 1D-b; amendment = reverse+repost; checkout requires balance 0.
### 1D-a implemented (approved slice): (a) cents-safe money (new domain/money.ts toCents/fromCents; calculateTotals + addPayment + settle now integer-cents); (b) immutable charge ledger — new FolioChargeStatus (POSTED/REVERSED/REVERSAL) + reversal_of_charge_id on folio_charges; new voidCharge() posts a negated REVERSAL row + flips original to REVERSED (never deletes/edits), idempotent re-void no-op, blocked on non-OPEN folio; totals stay a pure sum (original +X, reversal -X = 0); (c) atomic property-scoped folio numbering — new folio_number_counters table (backfilled from MAX existing suffix), nextFolioNumber() via own auto-committed INSERT...ON CONFLICT DO UPDATE RETURNING (replaces count()+1), format FO{YYMMDD}-{NNNNN} preserved; (d) REMOVED hidden syncPaidReservationFolio + creation-time BOOKING_MARKED_PAID auto-payment (paymentStatus/balance now purely ledger-derived). Reused EntityManager propagation; no new pool contention. Migration 20260829090000 (additive: enum, 2 cols default POSTED, counter table + backfill).
### Preserved unchanged: inventory locking, restriction validation, rate resolution, commercial snapshots/versioning, reservation lifecycle, 1C-d2 idempotency, reservation-code counter, txn/pool architecture. Room-charge hard-coded 3500 intentionally LEFT for 1D-b (still legacy). New endpoint POST /properties/:propertyId/folios/:folioId/charges/:chargeId/void (BillingManage). folio-response exposes charge status + reversalOfChargeId.
### Verified: tsc + nest build clean; migration applied (columns + counter backfill=4 = max suffix; 7 existing charges default POSTED); focused Jest billing 6/6 (2 old auto-payment specs replaced with de-coupling assertions) + reservation-workflow/reservations 87/87 (checkout regression). Live E2E /tmp/verify_d1a.py 17/17: folio number format + idempotent (no dup charges), 6 concurrent folio allocations all-unique, reversal audit trail (original REVERSED + negated REVERSAL row, balance restored), idempotent re-void, cents-safe partial-payment balance, settle blocked>0 / allowed=0, void rejected on non-OPEN. 0 residue, 0 non-POSTED leakage. Full Jest NOT run (testing-budget rule).
### NOT started: 1D-b onward. Note: reservation-code generation still has a pre-existing race for NON-external concurrent creates (absorbed for external via idempotency) — separate from billing, still open.

## 2026-08-17 — Phase 1D-b: Snapshot-driven ROOM charges + reconciliation — DONE & VERIFIED
- REMOVED the hard-coded ₹3500 room-charge from active billing (grep-confirmed gone from billing.service). ROOM charges are now generated EXCLUSIVELY from the reservation's ACTIVE reservation_rate_snapshot: amount = snapshot.totals.grandTotal, quantity = snapshot nights (or arrival→departure fallback), unitAmount = grandTotal/nights. Billing CONSUMES the snapshot (no independent rate calc); tax still via the existing TaxService hook (GST rework is 1D-c, untouched here).
- Schema (migration 20260830090000, additive nullable): folio_charges.rate_snapshot_id + rate_snapshot_version (+ index). Snapshot-driven ROOM charges carry both (provable audit link to exact commercial terms). NULL => legacy/manual charge, NEVER reconciled/overwritten.
- generateRoomChargesFromSnapshot(manager, folioId, propertyId, reservation, activeSnapshot): UNPRICED snapshot / no snapshot / zero grandTotal => posts NOTHING (no fabricated ₹0). Runs on the caller's txn manager (no nested txn, reuses EntityManager propagation, no pool contention).
- reconcileRoomChargesForReservation(propertyId, reservationId): OPEN-folio-only reverse+repost — reverses POSTED snapshot-driven ROOM charges (negated REVERSAL row + flip original REVERSED, 1D-a mechanism) then reposts from the CURRENT ACTIVE snapshot. Idempotent (no-op if live POSTED snapshot-driven charges already reference ACTIVE version). Transactional + cents-safe. Only touches rate_snapshot_id NOT NULL rows (legacy untouched). New endpoint POST /properties/:propertyId/reservations/:reservationId/folio/reconcile-room-charges (BillingManage). NOT auto-wired into reservation amendment (kept decoupled to avoid cross-module txn coupling; can be wired later).
- Preserved: all 1D-a invariants (immutable ledger, reversal-not-delete, ledger-derived balance, atomic folio numbering, no hidden auto-payment) and all Phase 1C behavior. childPricingService still injected (unused now; child pricing already inside snapshot grandTotal — avoids double-count).
- Verified: tsc + nest build clean; migration applied; focused Jest billing 6/6 (spec updated: snapshot repo mock added to constructor). Live E2E /tmp/verify_d1b.py 16/16 — PRICED snapshot -> ROOM charge (amount==grandTotal, id+version linkage, nights, unitAmount), v1->v2 reverse+repost (original REVERSED, reversal negated, new POSTED refs v2, NO inventory side effect), idempotent reconcile no-op, legacy 3500 charge preserved (only snapshot-driven reconciled to v3), UNPRICED -> zero fabricated charge, ledger-derived subtotal correct. 0 residue, 0 inventory invariant violations. NOTE: seed DB has NO active rate plans/applicabilities so the pricing engine yields UNPRICED live; PRICED snapshots were injected to test billing CONSUMPTION (pricing-engine output is Phase 1C, already covered). Full Jest NOT run (testing-budget rule).
- Accounting risk noted: reconciliation is manual (endpoint) — until wired into the amendment flow, an amended OPEN folio can show stale room charges until reconcile is called. Legacy ₹3500 charges on existing OPEN folios remain until an operator reconciles (they have no snapshot link so are never auto-touched). tax on ROOM charge still uses flat TaxService (1D-c will make it GST-correct + per-line snapshot).
- NOT started: 1D-c (Indian GST engine).


## 2026-08-17 — Phase 1D-b HARDENING: auto folio reconciliation wired into amendments — DONE & VERIFIED
- Wired reconcileRoomChargesOnManager(manager, propertyId, id) into BOTH commercial amendment flows: ReservationsService.update() (line ~554-567) and ReservationWorkflowService.postExtensionSnapshotVersion() (extendStay, line ~999-1025). Called inside the caller's existing transaction (EntityManager threaded, NO nested txn), guarded by `commercialChanged && rateSnapshotVersion != null && amendResult?.changed`. Closes the "manual endpoint only" accounting risk noted in the 1D-b base entry above.
- On a version-changing commercial amendment the OPEN folio now auto reverse+reposts its snapshot-driven ROOM charges (original -> REVERSED, negated REVERSAL row, new POSTED @ new version/amount). No-op when UNPRICED / no folio / non-OPEN folio / already at ACTIVE version. Legacy (rate_snapshot_id NULL) charges untouched. Everything rolls back with the amendment on 422.
- Verified: Jest reservations.service.spec.ts + reservation-workflow.service.spec.ts 88/88 (assert reconcile fires ONLY on version-changing commercial amendments, not on operational/no-op/rejected). Live E2E /tmp/verify_d1b_auto.py 24/24. testing_agent iteration_13: 14/14, 100% backend, 0 critical. All 8 mandated behaviors + edge cases; 0 residue; 0 inventory invariant violations. Full Jest NOT run (testing-budget rule).
- Deferred OPTIONAL/minor (pre-existing, NOT introduced here; left per user scope): (1) reconcile-created REVERSAL/repost rows have created_by_user_id=NULL (systemic gap in generateRoomChargesFromSnapshot, shared with folio-open path) — thread actorUserId to attribute; (2) commercial amendment on a SETTLED folio advances snapshot version while reconcile returns early by design — consider a drift signal or blocking commercial amendments once settled.
- NEXT: Phase 1D-c (Indian GST engine) — NOT started (awaiting user go-ahead).

## 2026-08-17 — Phase 1D-b post-hardening: SETTLED-folio commercial-amendment block — DONE & VERIFIED
- Centralized guard BillingService.assertCommercialAmendmentAllowedOnManager(manager, propertyId, reservationId): controlled 409 FOLIO_SETTLED_AMENDMENT_BLOCKED when the reservation's folio is SETTLED (billing can no longer reconcile it). OPEN/VOID/no-folio do NOT block. Single reusable rule (future authorized reopen/credit-note in 1D-d/1F relaxes it here). No auto-reopen, no mutation of settled history.
- Wired BEFORE amend in BOTH commercial flows: reservations.service.update() (inside `commercialChanged && rateSnapshotVersion != null`) and reservation-workflow postExtensionSnapshotVersion() (extendStay). Atomic rollback. Operational-only edits (notes etc.) continue normally.
- Attribution: reposted ROOM charge now honors actorUserId (generateRoomChargesFromSnapshot + reconcile threaded; REVERSAL already did). update()/extendStay do NOT yet pass actorUserId → full attribution DEFERRED to Phase 1F (needs controller plumbing; out of slice scope).
- Verified: Jest 96/96 (reservations.service + reservation-workflow + billing); live /tmp/verify_settled_block.py (OPEN still reconciles; date/rate-plan/extend blocked 409 w/ domain code; operational edit 200; rollback intact; 0 residue; 0 inventory violations). testing_agent NOT needed (no defect). NEXT: Phase 1D-c GST engine (awaiting go-ahead).


## 2026-08-17 — Phase 1D-c1: Indian GST engine — DONE & VERIFIED
- Configurable, effective-dated GST engine (no statutory rates hardcoded, ships EMPTY): `tax_rules` (property, charge type, HSN/SAC, tax %, optional per-unit tariff slab, effectiveFrom, isActive) + `GstService` + CRUD at /properties/:pid/rates/tax-rules (write SettingsManage, read SettingsView|BookingsView). Migration 20260901090000 (tax_rules + folio_charges.hsn_sac + folio_charges.tax_snapshot jsonb) applied.
- Per-line frozen tax snapshot on folio charges: taxable value, HSN/SAC, place of supply, CGST/SGST (intra-state) or IGST (inter-state) components+rates, rule ref. ROOM always INTRA_STATE, slab matched on per-night rate, rule resolved against ARRIVAL date; manual charges auto-compute (or honor explicit taxAmount override); REVERSAL rows carry negated snapshot. Folio totals add taxBreakdown {cgst,sgst,igst}. Cents-safe: total at configured rate then split (exact, components sum to line tax).
- Verified: Jest 16/16 (gst.service + billing.service); live /tmp/verify_gst.py 19/19; testing_agent iteration_14 30/30, 100% backend, 0 critical (suite /app/backend/tests/test_gst_engine_1dc.py). Fixed 2 in-scope findings (arrival-date effective basis; odd-rate exact split). Deferred by-design: negative credit/DISCOUNT lines allowed, no rule-overlap guard (deterministic), read visible to BookingsView; reconcile repost actor attribution still 1F.
- NEXT: Phase 1D-d (payments/refunds/deposits/settlement) OR 1D-e (invoice generation, immutable + credit notes). Invoice format intentionally OUT of 1D-c1.


## 2026-08-17 — Phase 1D-d: Payments, Refunds, Deposits & Settlement — DONE & VERIFIED
- Append-only, auditable payment ledger: `folio_payments` + type (PAYMENT|REFUND), reversal_of_payment_id, idempotency_key (unique per folio). Migration 20260915090000. addPayment/addRefund/settleFolio transactional with folio row lock (concurrency-safe, EntityManager propagation, no nested txn). Refunds = negative linked reversing rows, capped at original−priorRefunds (no over/double refund). Idempotency key + 23505 catch make retries & concurrent posts safe (verified 5 concurrent same-key → 1 payment).
- Balance/status DERIVED (charges+tax−net payments). Settlement requires EXACTLY zero (outstanding & credit both 400). Overpayment explicit: totals.creditBalance + paymentStatus OVERPAID, never discarded; blocks settlement until refunded. New totals fields creditBalance + paymentStatus (DUE/PARTIAL/PAID/OVERPAID). New endpoint POST /folios/:id/refunds (BillingManage). Checkout gating tightened to exact-zero (blocks credit too). GST snapshots + room reconciliation untouched.
- Verified: Jest 96/96 (no regression); live /tmp/verify_payments.py 22/22 (partial/multiple, exact settlement, under/overpayment, refund+cap+double-refund, idempotent retry, concurrent posting, derivation, checkout gating, GST unchanged). Zero residue. No invoice/PDF work.
- NEXT: Phase 1D-e (invoice generation, immutable + credit notes) — NOT started.


## 2026-08-17 — Phase 1D-e: GST Invoice, Credit Note, Finalization & Checkout Billing — DONE & VERIFIED — PHASE 1D COMPLETE
- Immutable `invoices` (TAX_INVOICE|CREDIT_NOTE; DRAFT/FINALIZED/VOID) + atomic `invoice_number_counters` (property+series+FY, ON CONFLICT DO UPDATE; never count()+1). Migration 20260930090000. FY numbering via existing buildInvoiceNumber/financialYearLabel + property_billing_config. Invoice content built by pure buildInvoiceSnapshot() from the FROZEN ledger (POSTED lines + GST snapshots + payments), asserts line-sum==ledger total, fully JSONB-snapshotted (seller GSTIN/state, buyer, reservation/folio refs, HSN/SAC + CGST/SGST/IGST + rates, totals, payments) so issued docs are immutable forever.
- finalizeInvoice: pessimistic folio lock, idempotent (one non-VOID tax invoice/folio, refinalize returns same, 23505 caught), requires exactly-zero settled balance + >=1 charge. FINALIZED immutable (void 409); corrections via linked auditable CREDIT_NOTE (full reversal, idempotent). Number allocated in-txn. Checkout finalizes the invoice on the same manager (finalizeInvoiceOnManager) from settled state — no nested txn, no recalculation.
- Verified: Jest 58/58 (pure snapshot builder + billing + workflow, no regression); live /tmp/verify_invoices.py 24/24 (atomic numbering, concurrent finalize→1, draft/finalize, immutability, totals==ledger, GST/HSN preserved, zero-balance gate, idempotent retry, credit-note linkage/reversal/idempotency, config-change immutability, checkout integration). Zero residue.
- PHASE 1D COMPLETE (a ledger, b snapshot ROOM, c GST, d payments/settlement, e invoices). Deferred: PDF/doc rendering (JSON contract ready), partial credit notes (full-reversal only), persisted DRAFT lifecycle (read-only preview used), guest GSTIN capture (field present/null), reconcile-repost actor attribution (1F). Out of scope: 1E, frontend, POS, deposit policy, payment receipts.
- MINIMUM BACKEND BEFORE FRONTEND: (1) PDF/document rendering OR let frontend render from the invoice JSON contract (data is complete); (2) optional formal InvoiceResponseDto (currently returns entity — acceptable); (3) optional aggregated billing-screen read endpoint (folio + invoices) — existing endpoints already suffice. No blocker remains for frontend to start against these APIs.


## 2026-08-17 — Phase 1E minimum backend (3 MUST-HAVE) — DONE & VERIFIED
- (1) assignRoom overlap guard verified already correct (ensureNoOverlappingAssignment) — no change.
- (2) Check-in workspace read model gains operational context: standardCheckInTime/standardCheckOutTime + derived earlyCheckIn/lateCheckout + roomAssigned (detection only, no enforcement/charge/inventory impact).
- (3) Existing EARLY_CHECK_IN/LATE_CHECKOUT policies wired: charge posted ONLY on explicit approval (earlyCheckIn/lateCheckout flags on PATCH check-in/check-out) via BillingService.postPolicyChargeOnManager (reuses GST + ledger, idempotent by description). Late charge committed in its own txn BEFORE the settle gate so a blocked checkout does not roll it back; early charge inside check-in txn.
- Preserved inventory/roomId separation, READY check-in, checkout->NEEDS_CLEANING, exact-zero checkout, policy resolution, GST. No new policy types/engine/schema.
- Verified: Jest 67/67; live /tmp/verify_lifecycle.py 14/14; zero inventory violations, zero residue.
- PHASE 1E MINIMUM BACKEND FOR FRONTEND: COMPLETE. Deferred to frontend/after-P1: arrival/departure/housekeeping queues (data already available), auto early/late enforcement, no-show auto-charge, housekeeping task workflow.

---
## F1 — Individual Booking pricing integration (2026-08-17)
**Root cause:** `/reservations/new` computed price locally (fallback rate ₹3,500/₹6,500, GST `?? 12%`) via `calculateBookingPricingPreview`; create used the backend rate snapshot → preview diverged from created reservation.
**Fix:** Added read-only backend quote endpoint `POST /properties/:propertyId/reservations/quote` (`ReservationQuoteService`) reusing the SAME path create persists (`ReservationPricingService`→`RateResolverService`) + `GstService` (tax_rules) + `PolicyResolverService` (INDIVIDUAL_DEPOSIT) + room-type occupancy enforcement. No DB writes. Frontend booking form + AvailabilityPage now consume the quote (debounced, loading/blocked states); removed all local rate/child/GST math and fabricated fallbacks.
**Verified:** Jest 7/7 (`reservation-quote.service.spec`); live E2E on real API — A DLX ₹5,900, B STE ₹7,900 (BAR default), GST 12% from tax_rules (CGST/SGST), C DLX 2+2 rejected, D STE 2+2 allowed; quote==created snapshot grandTotal (₹5,900); 3 quotes = 0 writes; inventory +1 on create / −1 on cancel; zero residue. Backend + web tsc/lint clean.
**QA config:** seeded idempotently via `stayos-api/scripts/seed-qa-config.ts` (BAR 5900/7900, GST ROOM 12% test rule, 6 stay policies, child bands 0-5 FREE/6-11 extra/12-17 adult).

### Local run notes (this session)
- Postgres 15 reused from `/app/postgres_data` on :5432 (had to recreate ephemeral dirs pg_notify/pg_logical/snapshots etc after resume).
- Nest API run manually: `cd /app/stayos-api && PORT=3002 npx nest start` → http://localhost:3002/api/v1 (`.env` created). Frontend deps installed (root `npm i`).

## F2 — Group Booking pricing integration (2026-08-17)
Fixed backend parallel pricing in `GroupRoomMixService`: replaced fabricated `estimateBaseRate` (3500/6500) with the default BAR `rate_plan_room_types` base rate (5900/7900) and swapped legacy flat `TaxService` for `GstService` (tax_rules ROOM 12%, intra-state, arrival-date). Group deposit already policy-driven (GROUP_DEPOSIT fixed ₹10,000). Updated `operations.module` (RatePlanRoomTypeEntity) + the group pricing unit test.
Verified (live, self-cleaning): suggestion DLX 5900/STE 7900, GST 12%, deposit ₹10,000; PENDING(ON_HOLD) consumes room-type inventory without roomId (19→18); confirm no double-consume (18); cancel releases (→19); rooming list + room assignment OK; zero residue; config intact. Jest 71/71 (operations); backend tsc clean. Frontend already consumes backend pricing (no change). Note: walk-in group still has its own `estimateBaseRate` fallback (out of F2 quote/hold path).

## F3 — Reservation Management (2026-08-17)
Backend lifecycle (confirm/cancel/no-show/amend/move/extend + inventory diff + snapshot/folio reconcile) verified correct. Frontend fixes: (1) `cancelReservation` now calls the dedicated `PATCH .../cancel` (was generic PATCH {status} which the backend ignores → never cancelled/released inventory); added `confirmReservation`/`markReservationNoShow`. (2) Wired the dead 'Confirm Booking' button (PENDING→CONFIRMED) as the obvious next action for PENDING. (3) Edit update payload now strips forbidden `status`/`source`/`ratePlanId` (fixed 400 regression). (4) `BookingStatus` type gained `NO_SHOW`; cancelled/no-show activity copy corrected; cancel test-ids added.
Verified: testing_agent UI E2E 3/3 PASS (confirm, cancel+release, edit-save); live API — PENDING consumes inventory, confirm no double-consume, cancel releases; edit old-payload=400 vs stripped=200; web tsc/lint clean. Env: Nest API on :8001 (ingress /api), Next dev on :3000, `apps/web/.env.local` → preview `/api/v1`.

## F4 — Arrival / Check-in (2026-08-18)
Backend check-in was already correct (CONFIRMED-gated, room+READY required, checklist blockers, early fee only on explicit earlyCheckIn:true, idempotent, →CHECKED_IN/OCCUPIED). Fixes: (BE) workspace `operational.earlyCheckInFee` now resolved from the EARLY_CHECK_IN policy (reuses PolicyResolver + normalizePolicyCharge; ₹1000 fixed) — CheckInService injects PolicyResolverService. (FE) added missing `operational` to CheckInWorkspaceDto; `checkInReservation` now sends `{earlyCheckIn}`; Step 4 shows a backend-driven early-check-in fee + explicit approval checkbox (gated on room READY) and passes approval to check-in; humanized raw room-status text (e.g. "Needs cleaning").
Verified: testing_agent UI E2E 3/3 PASS (early fee ₹1,000 shown, approval-gated single "Early check-in charge ₹1,000.00", →CHECKED_IN/Room OCCUPIED; unassigned→Assign action; NEEDS_CLEANING→actionable warning). Live API: approval posts exactly 1 charge, retry 400 no dup, no-approval=0 fee, MAINTENANCE/NEEDS_CLEANING block check-in. Jest check-in 9/9; web+backend tsc/lint clean; zero residue.

## F5 — In-house Operations (2026-08-18) — verification only, no code changes
Backend (moveRoom, extendStay, commercial amendment) already correct: move requires target READY + no overlap, releases old room (→NEEDS_CLEANING), keeps room-type entitlement unless type changes; extend is CHECKED_IN-gated, restriction-checked, reserves ONLY added nights via atomic applyDelta (throws INVENTORY_UNAVAILABLE, rolls back), amends snapshot + reconciles OPEN ROOM charges (reverse/repost, no dup). Frontend move/extend use correct routes with loaders, double-submit guards, actionable errors, auto-close.
Verified (focused live API, self-cleaning): (1) move→READY 200 old→NEEDS_CLEANING/new→OCCUPIED, roomType unchanged; (2) move→MAINTENANCE 400; (3) extend +1 200, snapshot version bumped, stays CHECKED_IN; (4) extend into full night 409 INVENTORY_UNAVAILABLE, dates unchanged; (5) amendment reprices snapshot, in-house folio 0 ROOM charges → no duplicates; (6) reconciliation discrepancies unchanged by F5 ops (64 pre-existing legacy before==after → 0 new); (7) zero residue. No files changed.
NOTE: 64 pre-existing inventory discrepancies exist from earlier phases' leftover reservations (unrelated to F5).


## F6 — Billing / Folio / Payments (2026-08-18) — COMPLETE & VERIFIED
Backend is authoritative for all folio math (BillingService): charges/tax computed on folio open; payments via POST /properties/:pid/folios/:folioId/payments support optional `idempotencyKey` (dup key returns existing folio, no double-post; unique-violation also absorbed); overpayment is allowed and produces a negative `balance`/positive `creditBalance` and BLOCKS settlement (400 "unrefunded credit balance … Refund the excess") until refunded; settle requires EXACT zero balance (>0 →400 outstanding, <0 →400 credit); payments/charges blocked on non-OPEN folios; reservation paymentStatus auto-synced (DUE/PARTIALLY_PAID/PAID).
Frontend (FolioPanel.tsx + billing-api.ts): PaymentModal generates a stable per-intent `idempotencyKey` (crypto.randomUUID) sent on every manual payment; Settle button disabled unless exact-zero (mirrors backend); overpaid folio shows "Credit (Overpaid)" indicator with credit amount; balance/paid/subtotal/tax all rendered from backend totals (no local math). Billing list KPIs + Total/Paid/Balance all backend-sourced.
Verified (focused live API on :8001, self-cleaning temp reservation+folio): partial pay → balance halves, OPEN; same idempotencyKey retry → payment count stays 2 (no dup); overpay → creditBalance surfaced + settle 400 credit-block; exact-full pay → balance 0.00 → settle → SETTLED (settledAt set); settle-with-outstanding → 400; payment on SETTLED folio → blocked. UI: /billing renders 5 OPEN folios with correct backend Total/Paid/Balance + KPI cards. No code changes needed this session (F6 frontend idempotency/overpayment/exact-zero-settle logic from prior session confirmed correct). Zero residue.

## F7 — Checkout (2026-08-18) — COMPLETE & VERIFIED
Backend checkOut (reservation-workflow.service) already authoritative: exact-zero folio gate (outstanding >0 →400, credit <0 →400), late fee via LATE_CHECKOUT policy posted in a pre-committed txn only when lateCheckout:true (idempotent by description → no dup on retry), CHECKED_IN→CHECKED_OUT, inventory released exactly once (wasReserved guard), assigned room→NEEDS_CLEANING, folio SETTLED + invoice finalized, snapshots immutable.
Changes: (BE) exposed backend-resolved LATE_CHECKOUT fee as operational.lateCheckoutFee in the check-in-workspace (check-in.service withLifecycleFees + resolveLateCheckoutFee, mirrors early-check-in; only when operational.lateCheckout true & CHECKED_IN; PolicyResolver + normalizePolicyCharge) + DTO field. (FE) checkOutReservation/useBookings/BookingDetailPage thread {lateCheckout} (rethrow on error); CheckoutModal fetches workspace, shows "Late checkout (standard HH:mm)" panel with backend fee + explicit staff approval checkbox, passes lateCheckout only when detected+approved, and on the post-fee outstanding-balance block refetches folio so staff collect then complete checkout (idempotent, no dup).
Verified (focused live API, self-cleaning fixtures + 1 UI screenshot): (1) settled checkout →CHECKED_OUT/room NEEDS_CLEANING/inv_reserved false/folio SETTLED/0 late charges; (2) outstanding →400 stays CHECKED_IN; (3) overpay/credit →400; (4) workspace late detection true + lateCheckoutFee ₹1000.00 (UI panel shows fee + approval); (5) no approval →0 late fee; (6) approval →exactly 1 late charge ₹1000; (9) retry →still 1 (no dup) then pay+checkout succeeds; (7,8,10) inventory released once, room NEEDS_CLEANING, reconciliation 72→72 (zero new). Backend tsc + 57 check-in/workflow specs green; web tsc + eslint clean on touched files. checkout_time restored to 12:00, zero residue.


## F8 — Housekeeping / Room Turnaround (2026-08-18) — COMPLETE & VERIFIED (no code changes)
Backend housekeeping.service already correct & complete: dashboard surfaces NEEDS_CLEANING/INSPECTION/READY/OCCUPIED/maintenance; assignEmployee (HK dept + ACTIVE guard, room stays NEEDS_CLEANING → not sellable); startCleaning (NEEDS_CLEANING + assigned → sets startedAt, HK status CLEANING, operationalStatus stays NEEDS_CLEANING → not sellable); completeCleaning (→INSPECTION, checklist-gated, only assigned employee); inspect APPROVE→READY / REJECT→NEEDS_CLEANING; markReady (INSPECTION→READY). OCCUPIED blocked in ensureStatus (ROOM_OCCUPIED); MAINTENANCE cannot reach READY (not in allowed statuses). All transitions transactional with audit+activity events. Sellability driven by operationalStatus===READY AND reservation/inventory entitlement (available-rooms excludes CHECKED_IN/conflicting/group-assigned rooms) — physical status independent of entitlement. Frontend housekeeping-api fully wired to all endpoints; board renders queue columns + assign/start/complete/inspect actions + staff access.
Verified (focused live API, self-cleaning DB-forced room states + 1 UI screenshot): (1) NEEDS_CLEANING appears (primaryAction ASSIGN_STAFF, not sellable); (2) assign → stays not sellable; (3) start → HK CLEANING/op NEEDS_CLEANING, not sellable; (4) complete → INSPECTION → inspect APPROVE → READY; (5) reservation-free room sellable only after READY (NEEDS_CLEANING/CLEANING/INSPECTION all not sellable); (6) MAINTENANCE → markReady & complete both blocked, stays MAINTENANCE; (7) OCCUPIED → start/complete/markReady all blocked (ROOM_OCCUPIED); (8) retry assign/start idempotent (startedAt unchanged), retry complete blocked (no dup); (9) reconciliation 72→72 (zero new); (10) zero residue (rooms restored READY). Note: room 306 carries a live CHECKED_IN demo reservation so it is correctly never sellable regardless of housekeeping — demonstrates entitlement independence. No backend/frontend code changed.


## F9 — Maintenance / Room Sellability (2026-08-18) — COMPLETE & VERIFIED
Backend maintenance.service mostly correct: create+makeRoomUnavailable → room MAINTENANCE; assign/check-in blocked for MAINTENANCE (ROOM_NOT_READY), OUT_OF_ORDER (ROOM_OUT_OF_ORDER), OUT_OF_SERVICE (ROOM_OUT_OF_SERVICE), OCCUPIED (ROOM_ALREADY_OCCUPIED) via ensureRoomAvailableForAssignment+ensureRoomReady; resolve/cancel → syncRoomAfterTicketClosed → stays MAINTENANCE if another blocking ticket active, else roomsService.returnToHousekeeping → NEEDS_CLEANING (never direct READY). Room-type inventory capacity = active room count, NOT reduced by physical MAINTENANCE → future entitlement independent of physical status.
FIX (bug found): resolve() and cancel() were not idempotent — re-resolving an already-RESOLVED ticket re-ran returnToHousekeeping and knocked a room housekeeping had already returned to READY back to NEEDS_CLEANING (retry duplicated maintenance effect / could re-block a cleaned room). Added idempotent early-return: resolve() no-ops when already RESOLVED, cancel() no-ops when already CANCELLED (no room re-sync). Files: maintenance.service.ts (+ spec: fixed happy-path seed IN_PROGRESS, added idempotent-retry test).
Verified (focused live API + 1 UI screenshot): (1) MAINTENANCE blocks assign, not assignable; (2) OUT_OF_ORDER blocks; (3) OUT_OF_SERVICE blocks; (4) capacity stays 19 with 1 room in maintenance, second future reservation still creatable (entitlement independent); (5) resolve → NEEDS_CLEANING; (6) housekeeping assign→start→complete→inspect → READY → assignable; (7) OCCUPIED assign blocked; (8) retry resolve after READY → stays READY (fix confirmed); (9) reconciliation 72→72; (10) zero residue (no leftover tickets/reservations, room READY). Backend tsc clean + maintenance specs 11/11; maintenance UI renders backend-sourced Open/InProgress/Resolved/HighPriority. No frontend code changed.


## F10 — Settings Runtime Verification (2026-08-18) — COMPLETE & VERIFIED (no code changes)
Proved every manager setting is consumed at runtime (backend source of truth); no defect, no duplicated/conflicting source found.
PROPERTY: check-in 14:00 drives earlyCheckIn (F4), check-out 12:00 drives lateCheckout (F7) — workspace operational.* read from property config. STAY POLICIES: individual quote deposit = INDIVIDUAL_DEPOSIT (30% → ₹3964.80 on payable); group deposit = GROUP_DEPOSIT ₹10,000 (F2 + present in resolved policies); CANCELLATION/NO_SHOW/EARLY_CHECK_IN/LATE_CHECKOUT all resolved into quote.policies + drive fees (F4/F7). RATES: default BAR resolved when ratePlanId omitted; DLX ₹5900/night (₹11,800/2n), STE ₹7900/night (₹15,800/2n); child bands 0-5 FREE / 6-11 RATE_PLAN_EXTRA_CHILD / 12-17 ADULT_PRICING consumed — set BAR DLX extra_child_charge=₹800 → 6-11 child priced ₹1,600/2n (rate-plan child amount consumed), restored; daily override + restrictions supported via RateResolver/RestrictionService (1C-b/1C-c). TAX: ROOM uses GstService (12%, HSN 996311, taxRuleId set, no legacy flat path); flipped rule 12→18% → quote reflected 18% (config consumed) while an already-posted folio ROOM tax stayed ₹1416 (historical immutable), restored to 12%. ROOM TYPES: maxAdults/maxChildren/maxOccupancy enforced in quote (adults 3>2 and children 2>1 both 400). Duplication: legacy properties.group_booking_deposit_* already dropped/consolidated into property_policies (1A) — single source. UI: Settings→Stay Policies reads back all rules from backend (Active), no hardcoded fallback.
Verified: focused live API (reservations/quote, group-room-mix-suggestions, tax flip, occupancy) + 1 settings UI screenshot; reconciliation 72→72; all config restored (tax 12%, extra_child 0); zero residue. No backend/frontend code changed.


## PRE-F11 — Inventory Reconciliation Cleanup (2026-08-18) — COMPLETE
Cause: the 72 discrepancies were entirely stale QA test residue in room_type_inventory — 68 ORPHAN_SOLD (stored sold>0 but expected 0) + 4 SOLD_MISMATCH (stored sold > expected), all pure OVER-count. Safety gate confirmed ZERO under-count (stored<expected) and ZERO capacity mismatches, i.e. no real domain defect / no oversell masking — so lifecycle & inventory rules were NOT changed.
Repair (QA-safe, property-scoped, idempotent; driven by the reconciliation service's own expected values): deleted 68 orphan inventory rows (expectedSold=0) and clamped 4 rows down to service-computed expected sold. No reservations/folios/payments/invoices/config touched. Post-repair reconciliation = 0 discrepancies, consistent=true; invariant check 0 rows with sold<0 or sold>capacity; all reservations preserved (62 CONFIRMED / 1 PENDING / 6 CHECKED_IN / 23 CHECKED_OUT / 6 NO_SHOW / 219 CANCELLED). Cleanup script removed (zero residue). Baseline for F11 is now clean (0).

